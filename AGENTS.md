# Agent directives — RotaFlow

**The directives live in [`CLAUDE.md`](./CLAUDE.md). Read that file.**

This one is a pointer, not a second copy. Until 30 August 2026 it held its own
near-duplicate of the same rules, and the duplication did what duplication
does: the two drifted. `AGENTS.md` was still describing live payment charging
and the platform billing console as "Phase 2" when both had shipped, because
nobody updating `CLAUDE.md` had reason to look here.

One file, so there is one thing to keep true. `CLAUDE.md` is the one the
harness loads automatically, so it is the one that gets read whether or not
anybody remembers to.

Everything that was here is there: deployment reality, styling, data access,
quality gates, scope discipline and the multi-tenancy guardrails.
