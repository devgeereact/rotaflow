# RotaFlow — SaaS Capability Register

**The single plan of record.** Five planning documents were merged into this file on
2026-08-29: the root project-memory file, the 13 August transformation plan, its 20 August
v2, the go-live decisions list, and the six shipped-feature plans and specs that lived
under a superpowers folder. They disagreed with the code and with each other. Do not start a sixth —
update this one.

## §0 How to use this file

**The rule: any PR that changes a capability's status updates its row in the same PR.**

This file records _what exists and what does not_. It does not restate the reference docs; it
links to them:

| Need                           | File                                    |
| ------------------------------ | --------------------------------------- |
| Coding standards               | `docs/RULES.md`                         |
| Folder layout, data flow       | `docs/ARCHITECTURE.md`                  |
| Tables, columns, RLS           | `docs/SCHEMA.md`                        |
| Hook contracts                 | `docs/HOOKS.md`                         |
| Design tokens                  | `docs/DESIGN.md` + `tailwind.config.ts` |
| Screen-by-screen build status  | `docs/SCREENS.md`                       |
| Metric definitions             | `docs/OBSERVABILITY.md`                 |
| Backup, retention, GDPR        | `docs/DATA_LIFECYCLE.md`                |
| Deploy                         | `docs/DEPLOYMENT.md`                    |
| Brand                          | `docs/BRAND.md`                         |
| Offline behaviour, per feature | `docs/OFFLINE-SPEC.md`                  |
| Release evidence               | `docs/PWA-RELEASE-GATES.md`             |
| How agent work is routed       | `docs/GEE-OS.md`                        |

**Automated freshness.** `.github/workflows/plan-drift-audit.yml` reads this file weekly via
`PLAN_DOC`, resolves every repo path cited below to an `EXISTS`/`MISSING` verdict _before_ any
model runs, and appends to the Drift Audit Log at the bottom. A row whose evidence path
disappears is flagged automatically. Keep citations to one path per row so the audit stays
focused.

**Read §2a before §7.** The gap register is long and reads like an engineering queue. Most of
it is downstream of one fact — production has no tenant — and the three cheapest things that
would move it are not code.

## §1 Status vocabulary

| Mark | Meaning                                                                       |
| ---- | ----------------------------------------------------------------------------- |
| 🟢   | Works end to end: UI → service → RPC/function → DB → RLS → audit → error path |
| 🟡   | Real implementation, important pieces missing                                 |
| 🔴   | No meaningful implementation                                                  |
| 🟠   | Exists, does not reliably do what it claims                                   |
| 🔵   | Works; not robust enough for a paying enterprise tenant                       |
| ⚪   | Present in schema, UI or docs only — not wired end to end                     |
| ⚫   | Deliberately deferred, reason given                                           |
| ❓   | Not audited. Never assume 🟢                                                  |

**A screen is not a capability. A table is not a feature. A toggle is not a control.** Do not
mark a row 🟢 because a button exists.

❓ rows name the exact test that would settle them. They exist because the repository cannot
answer everything — a real Stripe charge, an RLS fixture matrix against the deployed project,
real-device offline UAT and a restore-from-backup all need a live environment.

## §2 Verdict summary

Recounted 2026-08-31, after eighty-three pull requests landed (#178-#265); the full website and PWA audit of 2026-09-02, the production-readiness pass of 2026-09-04 and the delivery audit of 2026-09-05 and the reliability repairs of 2026-09-05 (RF-02 to RF-14) then took `main` to 126 migrations.

### What production actually holds, 2026-08-31

Measured, not assumed — several rows below used to quote counts from a dataset
that no longer exists, which is how a register starts lying.

|                       |       |
| --------------------- | ----- |
| Organisations         | **0** |
| Auth users / profiles | 1 / 1 |
| Staff profiles        | 0     |
| Clock events          | 0     |
| Subscriptions         | 0     |
| Support cases         | 11    |
| Audit rows            | 366   |

**There is no customer and no data.** That is the honest frame for everything
below: nothing here has been proved against a real tenant, several ❓ rows exist
precisely because they cannot be, and "it works" throughout this document means
"the mechanism is built and tested", not "somebody has used it".

**These counts are derived from the register, not maintained by hand.** Each is a straight tally of the `CAP-` rows in §4 by their status glyph, so the table and the register cannot disagree. The row count itself is deliberately not written here — `npm run check:docs` prints it (113 on 2026-08-31) and fails the build when the table and the rows part company, so a figure in this sentence would be the one number in the paragraph nothing checked. It had already drifted: it read 111 while the table summed to 113. The previous version was hand-edited on every change and had drifted: its columns summed to 120 against 111 rows, and it counted deferred and unaudited items that are not capability rows at all. The Δ column went with it — a delta nobody could reproduce was worse than no delta.

`❓` is a capability whose status is unaudited; the specific open questions are numbered `❓-001` to `❓-007` inline. `⚫` here is a _capability_ that is deferred — the seven things this project has decided not to build at all are listed separately in §9.

| Status                | Count |
| --------------------- | ----- |
| 🟢 Complete           | 98    |
| 🟡 Partial            | 0     |
| 🟠 Defective          | 0     |
| 🔵 Hardening required | 0     |
| ⚪ Surface only       | 0     |
| 🔴 Missing            | 1     |
| ⚫ Deferred           | 10    |
| ❓ Not audited        | 4     |

**Seven rows moved 🟡 → 🟢 on 2026-08-31 without new code, and that needs the same
scepticism.** Each was partial for a residual that is not the capability: a verification with
its own row (CAP-023 → CAP-023a), an open question belonging to another row (CAP-046 → GAP-033),
a document that is out of scope by decision (CAP-059's DPA), a duplication that is structural
and now checked (CAP-040), a deliberate design choice recorded as if it were an omission
(CAP-031's plain text, CAP-071's unchecked numbers), and a setting the owner has chosen
(CAP-097's `enforce_admins`). Carrying somebody else's open question inside a row made the
register look like it was waiting on engineering when it was not. Nothing was declared done
that is not done, and each row now names exactly what it does not cover.

**Nine rows moved 🔴 → ⚫ on 2026-08-31, and nothing was built to move them.** Read that as
what it is: those capabilities are exactly as absent today as they were yesterday. What changed
is that each now carries the decision and the reason instead of sitting in a list that implies
somebody will get to it. Five were already Phase 2 in `CLAUDE.md` (SSO/SCIM, public API,
outbound webhooks, payroll, per-tenant branding) and the register simply had not been told;
four are decisions taken here — QR clock-in (a shared credential replacing a geofenced one),
predictive insight (no data to fit), consent capture (the wrong lawful basis for employment
data), and product analytics (`docs/OBSERVABILITY.md` already argues that seven of eleven
metrics are a query away, and that a generic event system built first is the premature-dashboard
mistake). Every one names the condition that reopens it. The 🔴 count falling is bookkeeping,
not progress; what moved it the rest of the way was fourteen features shipping.

One of them was not only bookkeeping: CAP-068 had read "marketing copy only" for two days while
the copy stayed on a public page selling SSO on a £790/month plan. `0101` and `marketing.ts`
took it down.

**Overall maturity: 7.5/10**, revised up from 7 on 2026-08-31. The half point is for three
things that were structural rather than cosmetic: the notification queue now drains (it had
delivered nothing for a month), rate limiting exists where there was none, and the
multi-tenant isolation question the QA audit could not answer is now twenty pgTAP assertions
that run on every push.

What still holds it below 8, and none of it is code:

- **Production has no backups and no PITR, and the workaround is not running either.**
  Verified live: `pitr_enabled: false`, zero backups. And as of 2026-08-31 the nightly
  `pg_dump` that GAP-001 offered as a mitigation has run once and failed for want of its two
  secrets, so there is no dump either (GAP-036). It is a billing decision, and it is the single largest risk on this list — every
  other item here is recoverable.
- **No charge has ever completed.** Checkout, the portal and a signature-verified webhook all
  ship and are deployed; nobody has put a card through one (❓-002).
- **Nothing has been used on a real device.** The whole offline path, and push in particular,
  is verified by tests and by nobody's phone (❓-004, CAP-023a).

**The notification queue drains, 2026-08-31.** This section previously described a step that
could not be finished: `notification_function_secret` had to be set identically in two places,
neither of which can be read back, so whoever did not keep a copy could never reconcile them.
`0091` removes the problem instead of restating it — the secret is generated inside Postgres
with `gen_random_bytes`, lives in `vault` alone, and is checked by a verifier granted to
`service_role`. Nothing has to know its value, so nothing can hold a stale one.

**What is actually deployed, read from the project on 2026-08-31.** `supabase/functions/` does
**not** deploy on merge — it is a separate manual step, and a merged function that nobody
deployed is the most common way this register goes stale. Live versions: `ai-rota-assistant`
v26, `send-notification` v26, `test-smtp` v19, `create-checkout-session` v15,
`create-portal-session` v15, `stripe-webhook` v15, `send-invite` v5, `calendar-feed` v1.

Two of the eight run with `verify_jwt: false`, both deliberately and both with their own
boundary: `stripe-webhook` verifies a Stripe signature, and `calendar-feed` is fetched by
calendar clients that cannot present a header, so its token is checked in
`calendar_feed_shifts`, a function granted to `service_role` alone. The other six return 401
unauthenticated.

`STRIPE_MODE=live` was set on the project as part of that deploy. It had never existed — the old
code read "unset" as live, so the secret's absence was invisible until #163 made it a refusal.
Setting it preserves exactly the behaviour production already had. **`STRIPE_TEST_SECRET_KEY` is
still absent**, so the test-mode half of `0058` cannot be exercised, and the first charge (CAP-036)
cannot be a test charge until that secret exists.

The product is further along than its own documentation claimed in one direction and less far in
another. The scheduling core, the rota lifecycle, tenant isolation and the audit trail are
genuinely strong. The layers around them — delivery, entitlement, abuse control, and the
truthfulness of what the UI tells a user — are where the work is.

## §2a Why the ❓ rows are all still open, and what would actually shift them

Measured on production, 2026-08-31:

```
orgs 0 · users 1 · staff 0 · rotas 0 · clock_events 0
subscriptions 0 · notifications 0 · outbox sent 0 · push_subscriptions 1
```

**The seven ❓ rows are not seven problems. They are one, seen from seven angles: nobody has
ever used this product.** The outbox drain has run 2,590 times with zero failures and sent
nothing, because nothing has ever been enqueued, because there are no organisations. A test can
prove a mechanism works; it cannot produce the tenant that every remaining question needs.

That matters for how this register is read. Scanning §7 gives an impression of a long engineering
queue. Most of it is downstream of one absence, and three of the cheapest actions available are
not engineering at all:

| Action                                                             | Cost                             | Closes or unblocks                                                                                                                                                                |
| ------------------------------------------------------------------ | -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **Create one real organisation and run a week of rota through it** | nothing, and nobody's permission | ❓-001, ❓-003, ❓-004, ❓-006, ❓-007 — and gives GAP-001's backup something worth protecting                                                                                    |
| **Set `SUPABASE_DB_URL` + `BACKUP_PASSPHRASE`**                    | five minutes, free               | the backup half of GAP-036. The workflow is already written                                                                                                                       |
| **Set `STRIPE_TEST_SECRET_KEY`**                                   | free                             | ❓-002 and ladder stage 3 — without it not even a **test** card can go through Checkout, so the whole billing path is unproven for want of a secret rather than for want of money |

Set against that, the items that read as urgent are correctly parked: you do not buy
point-in-time recovery for an empty database (GAP-001), choose a support-email vendor before
there is anyone to support (GAP-035), publish a status page to nobody (GAP-024), or migrate to
React 19 for currency alone (GAP-037).

**The engineering is in better shape than the product's evidence.** That is the honest summary
of this file, and it is not a statement about code quality — it is a statement about the fact
that nothing here has been used in anger, and that no amount of further building changes it.

## §3 Maturity ladder

Each gate is phrased so two people could argue about whether it has been met.

| Stage           | Gate                                                                                                                                                      | Blocking                         |
| --------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- |
| 1. Safe         | A restore has been performed from a backup into a scratch project, and no screen tells a user something happened that did not                             | GAP-001 (043/044/045 closed)     |
| 2. Reliable     | Publishing a rota with the browser closed still results in a delivered, recorded notification                                                             | ❓-007 (GAP-026 closed)          |
| 3. Commercial   | A real charge appears in the live Stripe dashboard against a real subscription row, and adding a 16th staff member to a Starter org fails at the database | ❓-002 (GAP-008, BUG-052 closed) |
| 4. Professional | An organisation's staff receive mail from that organisation's own address, and the org can be branded                                                     | CAP-031, GAP-016                 |
| 5. Enterprise   | MFA can be required org-wide and enforced below the UI; a Trust Centre answers a security questionnaire without a human                                   | GAP-017, GAP-018                 |
| 6. Connected    | A third-party system can read a rota and receive a webhook when it changes                                                                                | GAP-019, GAP-020                 |
| 7. Intelligent  | A manager is warned about a coverage problem before it happens, from real data                                                                            | GAP-023                          |
| 8. Vertical     | One vertical's workflow is configurable without a code change                                                                                             | GAP-025                          |
| 9. Scaled       | A 500-staff, 20-site tenant loads the rota grid inside the performance budget                                                                             | HARDEN-007                       |

Stages are strictly ordered. Do not open stage 3 while stage 1 is unmet.

**Every stage above 3 is blocked by a decision, not by outstanding work**, and the difference
matters when reading the column: GAP-016 (stage 4), GAP-018 (stage 5), GAP-019 and GAP-020
(stage 6), GAP-023 (stage 7) and GAP-025 (stage 8) are all ⚫ in §7 — Phase 2, deferred on
evidence, or the owner's call — each with the condition that reopens it. Two exceptions inside
that: GAP-017 (stage 5) is genuinely half open, being the tenant half of MFA, and HARDEN-007
(stage 9) is a measurement nobody has taken. So the ladder above stage 3 is not a queue anybody
is working through; it is what the product would have to **decide** to become. Stages 1-3 are
the ones with work in them, and all three now turn on things this repository cannot do to
itself: a restore, a real charge, and a phone.

## §4 Capability register

### Scheduling

- [x] CAP-001 🟢 Rota builder — weekly/fortnightly/monthly grid, drag-and-drop, copy-previous-week
      `src/pages/app/RotaBuilderPage.tsx`
- [x] CAP-002 🟢 Rota lifecycle — draft/published/archived, DB-enforced, four guards no client can bypass
      `supabase/migrations/0061_rota_revisions.sql`, `0114_publication_boundary_in_the_database.sql` · GAP-039 closed #280 · 25 pgTAP assertions
- [x] CAP-003 🟢 Amendment flow — a published rota is amended via a revision, the original stays visible
      `supabase/migrations/0061_rota_revisions.sql` (`begin_rota_revision`)
- [x] CAP-004 🟢 Minimum cover — `publish_rota` refuses a shortfall, counting exactly as the builder
      does; the escape hatch is lowering that day's minimum, not a force flag
      `supabase/migrations/0080_enforce_minimum_cover.sql` · GAP-006 closed #192 · 9 pgTAP assertions
- [x] CAP-005 ⚫ Shift templates — removed rather than built. A pre-filled placement is a different
      feature from a shift type, and should be designed as one if it is ever wanted
      `supabase/migrations/0096_drop_shift_templates.sql` · BUG-051 closed #229
- [x] CAP-006 🟢 Repeating a week — `repeat_rota_weeks()` copies a week forward up to 26 weeks
      in one transaction, rather than twelve rounds of copy-navigate-paste and several hundred
      round trips that can each fail alone. The arithmetic is done in the SITE'S timezone: in
      UTC a 07:00 shift becomes 08:00 after the last Sunday in March, and nobody notices until
      somebody turns up late. It never writes over a published week or a draft already holding
      work, and reports how many it left alone
      `supabase/migrations/0107_repeat_rota_weeks.sql` · 9 pgTAP assertions
- [x] CAP-007 🟢 Conflict detection — leave, overlap, declared unavailability as hard blockers
      `src/lib/rotaInsights.ts`
- [x] CAP-008 🟢 Overnight shifts and DST — absolute-instant arithmetic, fall-back day tested
      `src/lib/schedulePeriod.ts`
- [x] CAP-009 🟢 Bank holidays — computed, not seeded: every rule is deterministic, so it never
      runs out in the December before the year everybody is planning. Three nations, because
      Scotland has no Easter Monday and takes the first Monday in August rather than the last.
      Set on Settings → Policies; a leave request names the bank holidays its range covers,
      before it is sent. One-off royal holidays cannot be derived and have a named place to go
      `src/lib/bankHolidays.ts` · 15 unit tests
- [x] CAP-010 🟢 Open-shift board — `/app/open-shifts` lists published, unclaimed, future shifts
      and `claim_open_shift()` takes one. The UPDATE re-checks "still open" in its WHERE, so two
      people tapping at once cannot both win. A shift clashing with the reader's own roster is
      shown and flagged rather than filtered — a board that silently hides rows leaves somebody
      staring at "nothing to cover" while a colleague sees four
      `supabase/migrations/0103_open_shift_claims.sql` · 11 pgTAP assertions
- [x] CAP-011 🟢 Clock in/out — GPS and manual, offline outbox, geofence recorded, duplicates refused server-side
      `src/pages/app/ClockInPage.tsx`, `supabase/migrations/0115_clock_event_sequence_guard.sql` · GAP-040 closed #281 · 6 pgTAP assertions
- [x] CAP-012 🟢 Offline clock-in honesty — a queued write is labelled "Saved on this device, not sent yet"
      `src/pages/app/ClockInPage.tsx` · BUG-043 closed #162
- [x] CAP-013 🟢 Sync state display — reads queue depth, not `navigator.onLine`
      `src/lib/clockRows.ts` (`syncStatusLabel`, 4 tests) · BUG-044 closed #162
- [x] CAP-014 🟢 Offline timestamp integrity — the guard still clamps, but records what the device claimed
      `supabase/migrations/0068_clock_event_reported_time.sql` · BUG-045 closed #172 · 9 pgTAP assertions · applied to production 2026-08-29
- [x] CAP-015 🟢 Replay idempotency — a key minted before the first attempt on all three offline writes,
      a partial unique index, and a collision counted as synced rather than dead-lettered
      `supabase/migrations/0081_outbox_idempotency.sql`, `src/lib/queuedWrite.ts` · BUG-046 closed #193, GAP-041 closed #282 · 5 pgTAP assertions · 4 unit tests
- [x] CAP-016 🟢 Dead-letter recovery — surfaced, retryable and dismissable, and scoped to the person
      who queued it; the retry is safe because the idempotency keys (`0081`) recognise a write that
      secretly landed
      `src/services/syncQueue.ts` · `src/components/FailedWritesNotice.tsx` · GAP-042 closed #283 · 40 unit tests
- [ ] CAP-017 ⚫ QR clock-in — **deferred with a reason, 2026-08-31.** The schema accepts `'qr'`
      and `docs/PRD.md` has called it deferred since V1. Worth writing down why, because "the enum
      already has it" keeps making it look nearly done: a QR code on a wall is a _shared_ credential.
      Anyone who photographs it can clock in from the car park, and the geofence that makes GPS
      clock-in trustworthy does not apply to a scan. It would be a weaker control replacing a
      stronger one. Reopens if a customer has a site where GPS genuinely cannot work — indoors,
      underground — which is the case it would actually be for
      `src/pages/app/ClockInPage.tsx` · P3
- [x] CAP-018 🟢 Timesheets — week approval, amendment, CSV export
      `src/pages/app/TimesheetsPage.tsx`
- [x] CAP-019 🟢 Missed clock-in — the dashboard keeps its live view, and a pg_cron job now alerts a
      manager without one being open, deduped per shift
      `supabase/migrations/0093_scheduled_alerts.sql` · GAP-013 closed #214 · 9 pgTAP assertions

### Communications

- [x] CAP-020 🟢 Notification dispatch — a rota publish enqueues its own notification in the same transaction; pg_cron drains it
      `supabase/migrations/0069_notification_outbox.sql` · BUG-047 #166, GAP-026 #174 · 10 pgTAP assertions
      ✅ **Draining since 2026-08-31.** `0091` moved the shared secret into `vault` alone, generated
      in-database; `send-notification` v20 verifies against it. Confirmed live: only `service_role` may ask,
      and the drain's value and the verifier agree.
- [x] CAP-020a 🟢 Leave, swap and announcement dispatch — triggers, in the same transaction as the decision;
      the unread reminder is an RPC, because no row changes when you press it
      `supabase/migrations/0087_server_side_dispatch.sql` · GAP-026 closed #200 · 13 pgTAP assertions
      ⚠️ `0088` follow-up: `announcement_audience` was still executable by `authenticated` — `0075` leaves a
      default ACL granting new functions to that role, so revoking `public`/`anon` alone is not enough (#201)
- [x] CAP-021 🟢 Notification preferences — org matrix and per-user switch both read on the send path
      `supabase/functions/send-notification/index.ts` · BUG-048 closed #165 · deployed v17, 2026-08-29
- [x] CAP-022 🟢 Channel record — the column says what kind of record the row is (`in_app`); per-channel
      outcomes live in `notification_deliveries`, which is where the question belonged
      `supabase/migrations/0082_notification_channel_is_in_app.sql` · BUG-049 closed #195
- [x] CAP-023 🟢 Web push — handler shipped and imported into the generated service worker,
      VAPID keys server-side, delivery outcomes recorded. **Whether one has ever arrived on a
      real device is CAP-023a**, which is a verification rather than a build and has its own
      row; leaving this one partial for that reason double-counted the same gap
      `public/push-sw.js` · CAP-023a / ❓-007
- [ ] CAP-023a ❓-007 A push has still never been seen arriving on a real device
      **Test:** subscribe on a phone, publish a rota, confirm the notification appears and opens `/app/notifications` · P1
- [x] CAP-024 🟢 Delivery tracking — one row per recipient per channel, with why a send was skipped
      `supabase/migrations/0067_notification_deliveries.sql` · GAP-004 closed #168 · 11 pgTAP assertions · migration applied and send-notification redeployed v18, 2026-08-29
- [x] CAP-025 🟢 Invite emails — sent automatically on **every** path, including the two sales-led ones
      that were still copy-a-link until 2026-08-30; the copyable link stays as the fallback
      `supabase/functions/send-invite/index.ts` · GAP-005 closed #198 · deployed v1, 2026-08-29
- [x] CAP-026 🟢 In-app notifications — per-recipient rows, no client insert policy
      `supabase/migrations/0002_rotaflow.sql`
- [x] CAP-027 🟢 Announcements with read receipts
      `supabase/migrations/0046_announcement_reads.sql`
- [x] CAP-028 🟢 Rota change notice — one message per affected person, naming the day and the before
      and after; people whose shifts did not change are not notified at all
      `supabase/migrations/0083_rota_amendment_diff.sql` · GAP-007 closed #197 · 8 pgTAP assertions
- [ ] CAP-029 ⚫ SMS — channel reserved in schema, no provider. A toggle that sends nothing is worse than none
      `src/lib/orgPreferences.ts`

### Organisation identity

- [x] CAP-030 🟢 Per-org SMTP — org sends from its own mailbox; password never readable by the client
      `supabase/migrations/0010_org_smtp_settings.sql`
- [x] CAP-031 🟢 Platform sender — `rotaflow.space` carries MX ×3, SPF, DKIM and DMARC, and
      since `0108` the messages are templated rather than `subject = title`. They are **plain
      text on purpose**: text renders everywhere, carries no tracking pixel, and cannot be the
      reason a rota notice lands in spam. HTML mail is not planned, and that is a decision
      rather than an omission
      `supabase/migrations/0064_rotaflow_space_domain.sql` · `0108` · P2
- [ ] CAP-032 ⚫ Per-tenant branding — **Phase 2, and `CLAUDE.md` has said so all along.** The
      register carried it as 🔴 "missing", which reads as an oversight; it is a scope decision. Not
      free either: a tenant logo is an upload path, a moderation question and a second place every
      email template has to be correct
      `src/types/database.types.ts` · GAP-016 · P3
- [x] CAP-033 🟢 Email templates — `notification_templates` with a platform default per event
      and an organisation's own wording overriding it, substituted in TWO passes so a value
      containing `{{…}}` cannot rewrite the email around it. In the database rather than the
      Edge Function on purpose: functions do not deploy on merge, so copy living in one is copy
      nobody can fix. **Deployed 2026-08-31: `send-notification` v26**, verified after —
      unauthenticated and wrong-secret both still 401, and `render_notification` returns a real
      message ("Sunnyvale Care: Your leave request was approved"). What is still unproven is
      the same thing that is unproven for push: nobody has read one of these in a mailbox. That
      is a real-recipient test, tracked with ❓-004, not a build
      `supabase/migrations/0108_notification_templates.sql` · 10 pgTAP assertions · deployed v26
- [x] CAP-034 🟢 Stripe Checkout, Billing Portal, signature-verified webhook
      `supabase/functions/stripe-webhook/index.ts`
- [x] CAP-035 🟢 Dual test/live credentials coexisting
      `supabase/migrations/0058_stripe_dual_mode.sql`
- [ ] CAP-036 ❓-002 No real charge has ever completed end to end
      **Test:** run one Stripe test-mode charge, then one live charge, and confirm the `subscriptions` row.
      Blocked on `STRIPE_TEST_SECRET_KEY`, which is not set on the project · **P0**
- [x] CAP-037 🟢 `STRIPE_MODE` fails closed — unset is refused with a 503 naming the secret, never assumed live
      `supabase/functions/_shared/stripe.ts` · BUG-052 closed #163 · deployed v11, 2026-08-29
- [x] CAP-038 🟢 Feature entitlements — every name in `plans.features` is now read by something:
      `ai_rota_assistant` in the Edge Function that spends the money, `advanced_reporting` on the
      Reports screen, and `gps_clock_in` removed because a gate every plan passes is not a gate
      `supabase/migrations/0090_entitlements_say_what_they_gate.sql` · BUG-064 closed #208 · 10 pgTAP assertions
- [x] CAP-039 🟢 Plan-limit enforcement — seats and sites refused at the database, inclusive of the cap
      `supabase/migrations/0070_enforce_plan_limits.sql` · GAP-008 closed #176 · 8 pgTAP assertions
- [x] CAP-040 🟢 Price source of truth — the duplication is structural and stays: the pricing
      page is unauthenticated and `plans` has an RLS policy requiring a session, so it cannot be
      fetched at render time. What was missing was anything that noticed a disagreement, and
      `marketing.test.ts` now reads the seeded rows out of the migrations — including later
      `update ... set description` statements — and fails when the two drift. The capability is
      "the page quotes what the customer is charged", and that is enforced
      `src/lib/marketing.test.ts` · BUG-053 closed #219
- [x] CAP-041 🟢 Subscription state — a failed payment now records when dunning opened and the date the
      subscription is at risk, and the billing page says so instead of showing a bare `past_due` badge
      `supabase/migrations/0098_subscription_grace_window.sql` · 6 pgTAP assertions

### Security

- [x] CAP-042 🟢 Tenant isolation — all 52 `public` tables have RLS enabled
      `supabase/migrations/0002_rotaflow.sql`
- [x] CAP-043 🟢 Security predicates — all six `SECURITY DEFINER` with `search_path` pinned
      `supabase/migrations/0028_support_access_gate.sql`
- [x] CAP-044 🟢 Support access — a time-boxed session is the actual RLS gate, not a log of intent
      `supabase/migrations/0028_support_access_gate.sql`
- [x] CAP-045 🟢 Cross-tenant isolation — the org A/org B matrix runs in CI: 20 assertions across every
      tenant table, both write directions, and the org-scoped RPCs, as `authenticated` with real JWT claims
      `supabase/tests/database/cross_tenant_isolation.test.sql` · ❓-003 answered 2026-08-31 #232
- [x] CAP-046 🟢 Rate limiting — org creation, invitations and the AI are capped, with a token
      ceiling and a per-org cap, and the limiter that takes a subject is revoked from every
      client role. The auth endpoints are GoTrue's, read and asserted by
      `scripts/check-auth-config.mjs` — which has never run green, for want of a
      `SUPABASE_ACCESS_TOKEN` secret (GAP-031, GAP-036). **That they are per-IP is GAP-033**, which is a CAPTCHA
      decision the owner has taken and tracked — carrying it here as well left this row partial
      for somebody else's open question
      `supabase/migrations/0085_rate_limiting.sql` · GAP-009 #199, GAP-031 #209, GAP-033
- [x] CAP-047 🟢 Grants wider than policies — `anon` holds nothing in `public` but schema usage;
      the default ACL that would have handed it back is narrowed too. **Now asserted rather than
      remembered (2026-08-31):** four pgTAP assertions check that every table in `public` has RLS
      enabled, that every table `authenticated` can read has a policy, and that neither `anon` nor
      `PUBLIC` holds a table privilege. `CLAUDE.md` has stated the first of those as a rule since
      the merge and nothing enforced it — a table added without RLS is not an error, it is every
      tenant reading every other tenant's rows in silence
      `supabase/migrations/0075_narrow_anon_privileges.sql` · `supabase/tests/database/rls_invariants.test.sql` · HARDEN-001
- [x] CAP-048 🟢 Predicate execute grants — `PUBLIC` and `anon` execute revoked on every callable
      function except `preview_invite`; trigger functions left, with the reason recorded
      `supabase/migrations/0075_narrow_anon_privileges.sql` · HARDEN-002 · P2
- [x] CAP-049 🟢 MFA — TOTP enrolment on the account screen, and `platform_settings.require_mfa`
      now decides something: `is_platform_admin()` requires an `aal2` session when it is on, so
      every console policy enforces it in one place. Turning it **on** is refused from a session
      that has not itself done MFA — the classic self-lockout — and turning it off is not, on
      purpose. Set to **off** in `0102`, because off is what was true: the flag had defaulted to
      on since `0027` with zero enrolled factors anywhere
      `supabase/migrations/0102_platform_mfa_is_real.sql` · GAP-017 · 11 pgTAP assertions
- [x] CAP-050 🟢 Sessions — `my_sessions()` lists every device on the account with its user agent,
      IP and last use, and `revoke_my_other_sessions()` signs the rest out. The row's "no server-side
      registry" was wrong: `auth.sessions` always had this and nothing read it
      `supabase/migrations/0100_own_sessions.sql` · 10 pgTAP assertions
- [x] CAP-051 🟢 URL scheme validation — both columns carry a database CHECK, so the browser is no longer
      the only thing between a `javascript:` link and an `<a href>`
      `supabase/migrations/0092_url_schemes_and_notification_retention.sql` · HARDEN-003 closed #213 · 7 pgTAP assertions

### Compliance

- [x] CAP-052 🟢 GDPR anonymisation — **and it was leaving the email address behind until
      2026-08-31.** `0011` clears the name, phone, photo, payroll id and login link; `0053`
      added `staff_profiles.email` two months later for account linking and nothing went back,
      so the record read "Deleted Member" with the person's email still on it — the strongest
      identifier of the lot, and usually the very thing an erasure request is made from. This
      row said "live-tested end to end", and it was: the test ran before the column existed.
      `0111` clears it, revokes any live calendar feed (a URL in somebody's phone would keep
      serving an erased person's shifts), and adds `erasure_retained_columns()` so the pgTAP
      test asserts over the COLUMN LIST — the next column added cannot survive an erasure the
      way this one did
      `supabase/migrations/0111_erasure_misses_email.sql` · 7 pgTAP assertions
- [x] CAP-052a 🟢 Subject-access export — **it covered eight datasets and missed the three a
      worker would ask for first.** Timesheets, overtime claims and pay rates were all absent,
      so somebody in a dispute about their hours or their money got their shifts and their
      holidays. Thirteen now, with the calendar feed token excluded and the reason printed in
      the file — it is a live credential, and emailing somebody a working URL to their rota
      would create the disclosure the export exists to answer for. `npm run check:export`
      covers both exports, so neither can drift again
      `src/services/gdprService.ts` · `scripts/check-export-coverage.mjs`
- [x] CAP-053 🟢 Organisation export and delete, with a pgTAP test — and the export was
      **materially incomplete until 2026-08-31**: 19 tables against 40 carrying an `org_id`,
      missing the organisation's invoices, staff inboxes, minimum-cover rules, integration
      history, GDPR log, support cases and the record of platform support access to its data.
      None of that was a decision; each table was added and nobody went back. Now 33 exported
      and 7 excluded with a reason printed in the file, and `npm run check:export` fails CI
      when a tenant table is in neither list
      `supabase/migrations/0063_delete_organisation.sql` · `scripts/check-export-coverage.mjs`
- [x] CAP-054 🟢 Retention enforcement — nightly `pg_cron`, running since 2026-08-21
      `supabase/migrations/0029_retention_enforcement.sql`
- [x] CAP-055 🟢 Audit trail — severity, scope, visibility, actor snapshot, immutability trigger
      `supabase/migrations/0016_audit_events.sql`
- [x] CAP-056 🟢 Audit labelling — every action the system writes has a name, with a readable fallback
      `src/lib/auditActions.ts` · BUG-054 closed #177 · 5 tests
- [x] CAP-057 🟢 Audit visibility — `visibility='both'` is readable by the tenant it concerns
      `supabase/migrations/0071_audit_visibility_both_readable.sql` · BUG-055 closed #177 · 7 pgTAP assertions
- [x] CAP-057a 🟢 Own-activity — the policy has allowed `actor_user_id = auth.uid()` since `0016`, and the
      writes carry it; the actorless rows are service-role actions, correctly labelled and not anybody's
      `supabase/migrations/0016_audit_events.sql` · BUG-063 closed #217 — the screen's claims were the bug
- [x] CAP-058 🟢 Consent capture — **reopened and built, 2026-09-04.** It was deferred on
      2026-08-31 with reasoning that is still correct about the half it covered: consent is the
      wrong lawful basis for employment data, the ICO's position is that it is rarely valid where
      there is a power imbalance, and nobody can withdraw consent to being rostered and still be
      paid. Nothing in the product asks a member of staff to consent to being scheduled, clocked
      in or located, and nothing here changes that.
      **What the row missed is that the premise it rested on was false.** It closed on the basis
      that there was nothing consent could be the basis for, citing a cookie page that said there
      was no third-party script and no tracking — while `src/lib/sentry.ts` was starting session
      replay and 20% performance tracing from `src/main.tsx` on every route, public pages
      included, before anybody had been asked. Replay and tracing are now removed, and what
      remains — crash reporting, and four interface preferences — is behind a granular banner
      with an equal accept and reject, a reopen control in the footer and in account
      preferences, a version that re-asks when the categories change, and a fail-safe read: an
      absent, corrupt or stale record permits nothing. The gate is in the write path
      (`isAllowed`), not in the banner, so an undecided visitor is untouched whether or not they
      answer. **Verified against a production build**, not just in tests: nothing reaches the
      Sentry endpoint before consent, and the envelope afterwards carries no replay, no
      transaction, no console breadcrumb and no query string
      `src/lib/consent.ts` · `src/components/consent/` · `e2e/consent.spec.ts` · BUG-068
- [x] CAP-059 🟢 Sub-processor list, AI transparency notice and security disclosure — published
      at `/legal/trust` with `security.txt`, each row citing where it can be checked. **The DPA
      is deliberately not drafted here and never will be**: it is a contract needing UK counsel,
      not a description of the system, and what it schedules is written and dated. Keeping the
      row partial for a document that is out of scope by decision made the register look like it
      was waiting on engineering
      `src/pages/legal/TrustPage.tsx` · `src/lib/subprocessors.ts` · GAP-014
- [x] CAP-060 🟢 Legal pages — Privacy, Cookies and Accessibility are written, from the code
      rather than from a template, because each is a DESCRIPTION of what the software does and
      a description can be checked. The cookie page can say plainly that there are none — no
      cookies, no analytics, no third-party script — and list every browser key with where it is
      set. Accessibility says what the gate covers AND that an automated scan finds a minority
      of real problems, rather than claiming an audit nobody has done.
      **All five pages are now written (2026-09-04), and two were wrong.** The Privacy page was
      six summary facts, which is a summary and not a notice: it named no operator, no lawful
      basis, no rights procedure and no regulator, and it asserted "no tracking" while session
      replay ran. It now carries a full draft notice whose every description cites the file that
      proves it, with the parts that cannot be settled from code — lawful bases, the Article 9
      condition, the transfer mechanism, the operator's registration — marked in place rather
      than omitted. Terms has left the placeholder for the same reason and with the same
      honesty: the descriptive two thirds (eligibility, roles, plans, billing, the grace window,
      deletion) are facts about a system that exists, and the commercial third says what has to
      be decided instead of inventing it. **Neither is publication-ready and both say so**
      `src/lib/legalFacts.ts` · `src/lib/privacyNotice.ts` · `src/lib/termsDraft.ts` · `src/pages/legal/` · P2
- [x] CAP-061 🟢 Data-residency claim — corrected when BUG-056 closed (#161) and verified again 2026-08-31:
      `DATA_LIFECYCLE.md` §2 now names both US processors and says the old claim was wrong
      `docs/DATA_LIFECYCLE.md` · BUG-056 closed #161 — this row was simply never flipped

### Integrations and API

- [x] CAP-062 🟢 CSV export — reports, timesheets, staff, and seven console screens
      `src/lib/csv.ts`
- [x] CAP-063 🟢 Calendar — a real subscription feed, not just a file: a per-person tokenised ICS URL a
      calendar re-reads, so an amended rota reaches the phone. The download stays for "send me this week"
      `supabase/functions/calendar-feed/index.ts` · `supabase/migrations/0099_calendar_feed_tokens.sql` · 8 pgTAP assertions
- [x] CAP-064 🟢 Connector catalogue — every connector is `planned` and `available = false`, so the
      console states it and `connect_integration` refuses one
      `supabase/migrations/0073_connectors_are_planned.sql` · BUG-057 · P2
- [ ] CAP-065 ⚫ Public API — **Phase 2 per `CLAUDE.md`, and the product already says so to users**:
      the account tab explains there are no tokens rather than showing an empty list. A public API is
      a versioning commitment, not an endpoint — once one customer scripts against it, the schema
      stops being ours to change, and this schema is still moving weekly
      `src/pages/app/account/TokensPage.tsx` · GAP-019 · P3
- [ ] CAP-066 ⚫ Outbound webhooks — **Phase 2 per `CLAUDE.md`.** The delivery machinery it would
      need now exists (`notification_outbox`, dedupe keys, a drained queue), so this is a smaller job
      than it was; it is still a promise to keep somebody's endpoint fed, with retries and a secret
      per subscription, and nobody has asked for it
      GAP-020 · P3
- [ ] CAP-067 ⚫ Payroll integration — **Phase 2 per `CLAUDE.md`, and the pricing page stopped
      selling it on 2026-08-31.** Enterprise listed "Payroll and HR integrations" when the whole of
      it is a `payroll_id` column and a timesheet CSV; `0101` and `marketing.ts` now say what is
      true. A real integration is per-provider (Sage, Xero, BrightPay each differ) and belongs to a
      named customer's payroll run, not to a roadmap
      `src/components/staff/StaffFormModal.tsx` · GAP-021 · P3
- [ ] CAP-068 ⚫ SSO / SCIM — **Phase 2 per `CLAUDE.md`, and no longer advertised.** This row said
      "marketing copy only" for two days while the copy stayed up: a £790/month plan whose summary
      read "Unlimited sites and staff, with SSO" and whose feature list promised "SSO with Microsoft
      365 or Google". Removed 2026-08-31 in `0101` and `src/lib/marketing.ts`. It is also gated
      outside this repository — Supabase SAML is a paid add-on above the free plan this project runs
      on — so it could not have been built to match the copy even in principle
      `src/lib/marketing.ts` · GAP-018 · P3

### Intelligence

- [x] CAP-069 🟢 Deterministic rota review — pure functions over the org's own rows
      `src/lib/rotaInsights.ts`
- [x] CAP-070 🟢 AI grounding for rota suggestions — every id validated against real rows before return
      `supabase/functions/ai-rota-assistant/index.ts`
- [x] CAP-071 🟢 AI announcement drafting — a date in neither the rota period nor the manager's
      own prompt is refused, and a name matching nobody on the roster is warned about. **Numbers
      are deliberately not checked**: "we need three more people on Saturday" is a statement
      about the business, not a claim about the rota, and a grounding rule that refused it would
      block the sentences managers most want to write. The two things a model actually
      hallucinates here — a date and a person — are caught
      `supabase/functions/ai-rota-assistant/grounding.ts` · BUG-058 closed #202 · 17 unit tests
- [x] CAP-072 🟢 AI cost control — `max_tokens`, a prompt cap, a context-size cap, per-user AND per-org
      hourly limits, and real token usage recorded on every audit row
      `supabase/migrations/0089_org_rate_limit.sql` · HARDEN-004 closed #204 · 6 pgTAP assertions · deployed v20, 2026-08-30
- [x] CAP-072a 🟢 AI resilience — a fallback model is configured and deployed: `google/gemini-3.5-flash-lite`,
      a different provider, so an outage routes away rather than returning 502
      `supabase/functions/ai-rota-assistant/index.ts` · HARDEN-010 closed #233 · deployed 2026-08-31
- [ ] CAP-073 ⚫ Predictive insight — **deferred, 2026-08-31, for a reason that is about data
      rather than difficulty.** Coverage risk and absence patterns are predictions, and production
      holds zero clock events and zero organisations. A model or heuristic fitted to nothing is a
      confident-looking number with no evidence under it, which for a rota is worse than no number.
      Reopens when a real tenant has a few months of history — the same condition CAP-086 waits on
      GAP-023 · P3

### Platform administration

- [x] CAP-074 🟢 Platform console — 19 screens, `RequirePlatformAdmin`, four role-narrowed routes
      `src/lib/adminNav.ts`
- [x] CAP-075 🟢 Incident register
      `supabase/migrations/0021_platform_incidents.sql`
- [x] CAP-076 🟢 Feature flags — `my_feature_access` returns them and the AI gate honours a flag as well
      as a plan; `beta_integrations` is retired rather than left describing connectors that do not exist
      `supabase/migrations/0090_entitlements_say_what_they_gate.sql` · BUG-064 closed #208
- [x] CAP-077 🟢 Demo constants in the console — the file is deleted; every console figure is measured
      or reads "Not sampled"
      `src/pages/admin/AdminPlatformHealthPage.tsx` · BUG-059 · P2
- [x] CAP-078 🟢 Platform health — `pg_cron` probes every five minutes, unattended; the summary view
      answers uptime and latency from separate sources rather than averaging them
      `supabase/migrations/0076_scheduled_health_probe.sql` · GAP-011 · P2
- [x] CAP-079 🟢 Support CSAT — the requester rates a resolved case from `/app/help`; the RPC now
      refuses an out-of-range score in its own words rather than through the column CHECK
      `src/pages/app/HelpPage.tsx` · BUG-060 · P2
- [x] CAP-080 🟢 Support cases — the requester sees their own cases and the non-internal thread
      (BUG-060), **replies into it** (GAP-012, on an open or a resolved case but not a closed one,
      because a closed case is the one state nobody is watching), and there is now a promise with a
      clock on it: targets per priority as rows,
      a deadline computed from when the case ARRIVED, and met/due-soon/breached states. Two
      decisions written down rather than assumed — the clock does **not** pause while waiting
      on the customer (the standard alternative lets a case sit three weeks and still report as
      within target), and escalating a late case recomputes from arrival, so a breach cannot be
      escalated away. **Email ingress is a separate capability, not part of this row**
      `supabase/migrations/0110_support_sla.sql` · GAP-012 closed, GAP-035 (email ingress) opened · 9 pgTAP assertions
- [x] CAP-081 🟢 Staff records — contract type, hours, skills, payroll id, documents, emergency contact
      `src/components/staff/StaffFormModal.tsx`
- [x] CAP-082 🟢 Leave, overtime and swap request/approval flows
      `src/pages/app/LeavePage.tsx`
- [x] CAP-083 🟢 Open swap board — claim an untargeted swap, RLS-enforced
      `supabase/migrations/0044_swap_open_board.sql`
- [x] CAP-084 🟢 CSV import — Team → Import reads a spreadsheet, shows every row back with its
      own problems, and creates the ones that are clean. Hand-written parser (quoted commas,
      quoted newlines, CRLF, Excel's BOM) rather than a dependency; refuses an ambiguous date
      rather than guessing the order, and catches a second run of the same file by email. No
      logins created and nothing emailed — sixty invitations off a file somebody was still
      checking is not recoverable
      `src/lib/csvImport.ts` · `src/components/staff/ImportStaffModal.tsx` · 19 unit tests
- [x] CAP-085 🟢 Leave-year rules — the calendar-year hardcode is gone (the year start is a
      policy, a joiner is pro-rated, unused days carry over up to a cap), and **half days now
      exist**: two booleans rather than a portion enum whose values would mean different things
      at the two ends of a range. A request never costs zero — a single day booked as both a
      late start and an early finish is still half a day, and a request worth nothing would
      pass every entitlement check ever written. A range clipped at the year boundary keeps
      only the half day that falls inside it, so the two years still sum to what was taken
      `src/lib/leaveYear.ts` · `supabase/migrations/0109_half_day_leave.sql` · 7 pgTAP + 26 unit
- [x] CAP-086 🟢 Pay rates and labour cost — `staff_pay_rates` with its own policies, because a
      rate on `staff_profiles` would publish everybody's pay to every colleague (that table is
      readable by any member). A history, not a value: a raise in April must not rewrite what
      March cost. `labour_cost()` returns money without handing out the rates it used, and
      COUNTS the people it could not price rather than treating them as free
      `supabase/migrations/0104_pay_rates.sql` · 11 pgTAP assertions
- [x] CAP-087 🟢 Overtime — still a declared number, now shown beside what the clock recorded for that
      person that day, so an approver judges it against something. Evidence, deliberately not derivation
      `supabase/migrations/0097_overtime_evidence.sql` · 7 pgTAP assertions
- [x] CAP-088 🟢 Document expiry — alerts a manager on a schedule, once per document per expiry date
      `supabase/migrations/0093_scheduled_alerts.sql` · GAP-013 closed #214
- [x] CAP-089 🟢 Multi-location workers — **and the row's proposed fix would have been wrong.**
      It said `staff_profiles` has no `location_id`; adding one cannot express a person who
      works two sites, which is the thing being asked for. The real shape: a site is inherited
      from the DEPARTMENT, so everybody has exactly one by construction. `staff_locations` is a
      set, with a trigger refusing a row whose org disagrees with either parent. Nothing
      recorded still falls back to the department, so an organisation that ignores it sees no
      change — and it never refuses an assignment, only answers "who can cover here"
      `supabase/migrations/0105_staff_locations.sql` · 9 pgTAP assertions
- [x] CAP-090 🟢 Delegation — cover that expires by time rather than by somebody remembering
      to undo a promotion. Enforced inside `has_org_role`, so every managerial policy honours
      it at once. **It confers `manager` and never `owner`**: an owner going away for a
      fortnight is not a reason to hand somebody the ability to delete the organisation or
      change billing, and the screen says so. It cannot be chained — `delegate_role` reads the
      caller's own membership, not `has_org_role`, which now returns true for a delegate
      `supabase/migrations/0106_role_delegation.sql` · 10 pgTAP assertions
- [x] CAP-091 🟢 Ownership transfer — one transaction, promoting and demoting together, from the
      owner-only settings page; the outgoing owner stays on as a manager
      `supabase/migrations/0095_ownership_transfer_and_duplicates.sql` · 10 pgTAP assertions
- [x] CAP-092 🟢 Duplicate detection — one account cannot hold two staff records, and two sites cannot
      share a name. Payroll ids were already unique since `0041`, which this row did not know
      `supabase/migrations/0095_ownership_transfer_and_duplicates.sql`
- [x] CAP-093 🟢 Unified approvals queue — `/app/approvals` lists leave, swaps and overtime
      together, oldest first, and decides them in place. A swap still waiting on a colleague is
      deliberately excluded: a queue with rows the reader cannot clear stops being read. The
      swap decision moved into `swapService.decideShiftSwap` so the two screens that approve
      swaps cannot drift apart on "approve, then reassign, but only if nobody decided first"
      `src/lib/approvalQueue.ts` · `src/pages/app/ApprovalsPage.tsx` · 9 unit tests
- [x] CAP-094 🟢 Onboarding — the org is still created at step 1 by design, and an unfinished wizard now
      resumes at step 2 instead of bouncing to the dashboard forever
      `supabase/migrations/0094_onboarding_completion.sql` · GAP-015 closed #218 · 6 pgTAP assertions

### Reliability and operations

- [ ] CAP-095 🔴 Production backups — **the mechanism is built and running; two secrets are
      missing, and only the owner can add them.** `pitr_enabled: false` and an empty backup list
      on the Supabase side (a paid feature), and `.github/workflows/backup.yml` runs nightly and
      fails in seven seconds with `Missing secret(s): SUPABASE_DB_URL BACKUP_PASSPHRASE` — last
      at 08:53 on 2026-08-31. That failure is deliberate and correct: a backup job that skipped
      quietly would be worse than none. Nothing an agent can close — one is a database
      credential — and it is the last 🔴 in this register and the largest single risk in the
      product, because every other item here is recoverable
      `.github/workflows/backup.yml` · GAP-001 · **P0, owner only**
- [x] CAP-096 🟢 Migration safety gate — nine destructive statement classes must carry `-- SAFETY(<rule>)`
      in the migration; verified to fire on all nine and stay silent on all ten written today
      `scripts/check-migration-safety.mjs` · GAP-002 closed #194
- [x] CAP-097 🟢 Branch protection — `verify`, `e2e`, `db-tests` and `e2e-authenticated` are all
      required on `main`. `enforce_admins` stays **off by the repository owner's decision**, so
      the row is complete: a setting somebody has chosen is not an outstanding defect, and
      recording it as one made every reader look for work that nobody intends to do
      `.github/workflows/ci.yml` · GAP-003
- [x] CAP-098 🟢 CI gates — typecheck, lint, format, the unit suite, build, SW assertion, bundle budget,
      migration safety, dependency audit. The test count is not quoted here: it was "636" and then "684",
      and a number nothing verifies is a number that drifts
      `.github/workflows/ci.yml`
- [x] CAP-099 🟢 E2E coverage — 26 authenticated screens rendered and scanned in BOTH themes, plus a test
      that genuinely signs up and creates an organisation against a local Supabase stack
      `e2e/app-surface.spec.ts` · `e2e/authenticated-loop.spec.ts` · GAP-010 closed #222
- [x] CAP-100 🟢 Component tests — three suites now, chosen by the same rule rather than by
      coverage: the components whose WORDING is load-bearing. `FailedWritesNotice` (which
      sentence a carer gets decides whether they think they are clocked in), `RepeatWeekModal`
      (the number that decides how many hundreds of rows get written), and `LabourCostCard`
      (a total that quietly prices somebody at zero is wrong in the direction that looks fine —
      the warning is the whole safeguard). Still no `jest-dom`, deliberately
      `src/components/**/*.test.tsx` · 20 tests across 3 files
- [x] CAP-101 🟢 Bundle size gate — four budgets plus a hard "no DEV page ships" invariant,
      enforced in `verify` and printed on every run
      `scripts/check-bundle-size.mjs` · `bundle-budget.json` · P2
- [ ] CAP-102 ⚫ Product analytics — **deferred, 2026-08-31, on this project's own recorded
      reasoning rather than on effort.** `docs/OBSERVABILITY.md` §"What this means for Phase 3"
      says seven of eleven metrics are a query away from real, and that building a generic
      event system before checking which already have their data is "exactly the premature
      dashboard mistake". Production holds zero organisations, so a `product_events` table
      would collect nothing and be designed against nobody's behaviour. It would also reopen
      CAP-058: analytics is one of the two things for which consent genuinely is the lawful
      basis. **Reopens with the first design partner**, and the four events that will actually
      need capture are already named in OBSERVABILITY — rota-session starts, schedule views,
      an explicit AI-suggestion outcome, and Web Vitals
      `docs/OBSERVABILITY.md` · P3
- [x] CAP-103 🟢 Error tracking — Sentry in the client, and in all eight Edge Functions via
      `_shared/sentry.ts`; the Stripe webhook no longer fails into a log nobody reads
      `supabase/functions/_shared/sentry.ts` · HARDEN-005 closed #216 · deployed 2026-08-31
- [x] CAP-104 🟢 Query shape — 16 composite indexes cover the predicates the app issues; the
      builder loads every location's week in two queries, not two per location
      `supabase/migrations/0072_hot_path_indexes.sql` · HARDEN-006 · P2
- [x] CAP-105 🟢 Concurrency on approvals — the UPDATE carries a state predicate, so the losing
      writer changes zero rows and is told so
      `src/services/reviewConcurrency.test.ts` · BUG-061 · P2
- [ ] CAP-106 ❓-004 Real-device offline UAT has never been performed
      **Test:** sign up → publish → no-signal clock-in → reconnect → correction, on real hardware · P1
- [ ] CAP-107 ❓-005 No restore has ever been performed from a backup by a person — **but the
      nightly job now rehearses one.** `verify-restore` decrypts the dump it just took, restores
      it into a throwaway PostgreSQL and asserts a database came back rather than a file: 40+
      tables, and `organisations`, `shifts`, `clock_events` and `audit_logs` all present with
      their row counts printed. That closes the failure where the job writes an encrypted file
      for months and the first person to open it finds it never contained anything. It does NOT
      close this row: a logical dump into stock Postgres proves the DATA survives, and auth,
      storage and RLS are Supabase's and are not in the dump
      **Test:** restore a snapshot into a scratch project and diff the row counts · **P0**
- [x] CAP-108 🟢 `pg_cron` retention job — verified scheduled on production as `rotaflow-retention @ 15 2 * * *`
      `supabase/migrations/0029_retention_enforcement.sql` · ❓-006 answered 2026-08-29

## §5 The recommendation we rejected

An enterprise review recommended replacing per-org SMTP with a transactional provider carrying
per-tenant domain claim, DNS verification, DKIM signing and reputation isolation.

**Rejected.** `org_smtp_settings` already reaches the stated goal more cheaply: each organisation
supplies its own SMTP account, `send-notification` resolves org SMTP → global fallback → skip, and
`test-smtp` is the only writer of `verified_at`, so "saved" and "known to work" stay distinct.
Mail leaves the organisation's own server, which already holds valid SPF and DKIM and carries its
own reputation. The proposed workstream would rebuild that at significant cost.

The platform half is now better than that review assumed. Migration `0064` records the real
cause of the old deliverability failure: a **missing MX**, not missing SPF/DKIM. The old host
signed outbound mail correctly while silently dropping every reply. `rotaflow.space` carries MX
×3, one SPF, one DKIM and one DMARC.

The same review's pillar tree assumed QR clock-in, calendar sync, working push and eight live
connectors. None of those exist (CAP-017, CAP-063, CAP-023, CAP-064). A structure drawn over
absent features is not an information architecture.

## §5a The twelve go-live decisions, reconciled

The go-live decisions list (6 August, since deleted) posed twelve decisions only the owner could make.
Half were taken and the file never said so. Reconciled here against the code so the
resolved ones are not re-litigated.

| #   | Decision                                                                 | Status                                                                                                                                                                                                                                                     |
| --- | ------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Platform staff can read every tenant; a support session does not gate it | ✅ **Resolved** — `0028` redefines `is_org_member`/`has_org_role` onto `has_support_access`, so a time-boxed session is now the actual RLS gate                                                                                                            |
| 2   | Demo data attached to real organisations                                 | 🟡 Dataset torn down 2026-08-14; seed scripts deleted in `#120`. `is_demo` survives as an unread column                                                                                                                                                    |
| 3   | Feature flags control nothing                                            | ✅ **Resolved** — `my_feature_access` returns them and the AI gate honours one (CAP-076). This row still read "🔴 Still true" against a capability marked 🟢; corrected 2026-09-02                                                                         |
| 4   | No payment provider                                                      | ✅ **Resolved** — Stripe shipped (`0050`, `0058`). Open sub-item: no charge has completed (CAP-036)                                                                                                                                                        |
| 5   | Retention is a published policy nothing enforces                         | ✅ **Resolved** — nightly since 2026-08-21 (`0029`, fixed by `0057`)                                                                                                                                                                                       |
| 6   | Three RPCs callable from nowhere                                         | ✅ Resolved 2026-08-30. All three now have callers — `rate_support_case` was the last, from `/app/help` (CAP-079)                                                                                                                                          |
| 7   | Health and uptime figures are seeded, not measured                       | 🟡 Now written — but only when an admin opens the page (CAP-078)                                                                                                                                                                                           |
| 8   | Eleven console screens show demonstration figures                        | ✅ Resolved 2026-08-30. `adminOverviewDemo.ts` is deleted; every console figure is measured or reads "Not sampled" (CAP-077)                                                                                                                               |
| 9   | An orphan `incident_events` table in production                          | ✅ **Resolved** — dropped by `0028`                                                                                                                                                                                                                        |
| 10  | Public repo, `main` unprotected                                          | 🟡 Four checks are now required — `verify`, `e2e`, `db-tests` and `e2e-authenticated` (GAP-003 closed 2026-08-30, CAP-097). `enforce_admins` remains off by the owner's decision. This row still said the last two were not required; corrected 2026-09-02 |
| 11  | Support cases have no way in                                             | 🟡 `/app/help` opens one; no email ingress and no customer-facing thread (CAP-080)                                                                                                                                                                         |
| 12  | The public status page                                                   | ⚫ Correctly deferred (GAP-024)                                                                                                                                                                                                                            |

**Settled, recorded so it is not re-opened:** every write to the platform tables goes through
a `SECURITY DEFINER` function with table grants revoked, so reference numbers, resolution
notes and audit rows cannot be skipped by a client. The marketing site carries no invented
traction, testimonials or logos.

**Still open, and not a code question:** a competitor ships under the RotaFlow name in the
same category and holds the `.app` domain. Naming has never been revisited. `rotaflow.space`
is ours and live since 2026-08-29.

## §5b Shipped-feature design records

Three features were built from written specs, since merged here: Stripe billing (`0050`,
`0058`), admin-assisted organisation creation (`0052`) and admin billing real data
(`2e1b924`). Their rationale now lives in the migration headers, which are the durable
record — each explains what it does to existing rows and why. The separate plan and spec
plan and spec files were deleted on 2026-08-29: all three specs still read
"pending implementation plan" for features that had shipped, and all 99 of their task
checkboxes were unticked against code that was already on `main`.

**Two deliberate loose ends.** Applied migrations are frozen, so four of them still name
`docs/audit01.md` (deleted 2026-08-04) in comments, and `0050_stripe_billing.sql` writes a
**column comment into production** that cites one of the deleted spec files. Editing an
applied migration risks a checksum mismatch on `supabase db push` for a comment nobody
executes, and correcting the column comment would mean a migration that auto-applies to a
database with no backups. Both are recorded here instead of chased.

## §6 Defect register

New findings start at BUG-043; 001–042 are in use across `docs/QA-AUDIT-REPORT.md`, migration
headers and source comments.

| ID          | Feature                                    | Expected                                     | Actual                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Severity |
| ----------- | ------------------------------------------ | -------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| ~~BUG-043~~ | Offline clock-in                           | Queued state shown until synced              | **CLOSED #162** — the status row now carries "Saved on this device, not sent yet" while the latest event is unsynced                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | —        |
| ~~BUG-044~~ | Sync indicator                             | Reflects queue depth                         | **CLOSED #162** — `syncStatusLabel()` reads queue depth; "Synced" only when the outbox is empty                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | —        |
| ~~BUG-045~~ | Clock timestamp                            | The time the user clocked in                 | ✅ **Closed by `0068`, struck 2026-08-30.** The row stayed open after the fix landed, which is the register failing at its one job. `0068` keeps the clamp — it is a payroll-fraud control — and adds `event_at_reported`, so a replayed offline clock-in records what the device claimed alongside what was stored. The case it hits in practice is a carer with three days of no signal, not fraud.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | —        |
| ~~BUG-046~~ | Outbox replay                              | Exactly once                                 | ✅ **Closed 2026-08-30.** `0081` adds `client_event_id` plus a partial unique index to `clock_events`, `leave_requests` and `shift_swaps`, and the client treats a collision on those indexes as **synced** rather than dead-lettered — 23505 classifies as permanent, so without that a person would be told a clock-in failed that is sitting in their timesheet. The key is minted **before the first online attempt**, not at enqueue: stamping at enqueue would close the small hole (insert succeeded, `outboxRemove` did not) and leave the larger one open — the request lands, the **response** is lost, `classifyFailure` calls it transient and the catch queues a row that already exists, which `ClockInPage`'s own comment describes as routine on ward wifi. `supabase/tests/database/outbox_idempotency.test.sql`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | P1       |
| ~~BUG-047~~ | Notification dispatch                      | Delivered or recorded as failed              | **CLOSED #166** — a failed dispatch queues in the outbox and retries. _The original diagnosis here was wrong: a rota cannot be published offline, because `publish_rota` is an RPC. The real failure is the second request to `inn.gs` failing after the publish already landed — a hostname content blockers drop._                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | —        |
| ~~BUG-048~~ | Notification preferences                   | Honoured at send time                        | **CLOSED #165** — org matrix and per-user switch both read in `send-notification`; opt-out drops the recipient                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | —        |
| ~~BUG-049~~ | `notifications.channel`                    | Records how it was delivered                 | ✅ **Closed 2026-08-30.** Two things were wrong, not one: the value was the constant `'push'`, and the CHECK was `('push','email','sms')` — so there was no value the column _could_ honestly hold, because every row this table has ever had is an **in-app** notification. `send-notification` inserts it only inside `if (orgChannels.in_app)`, as its own comment says. `0082` adds `in_app`, writes it, and backfills the mislabelled rows. The column now records what KIND of record this is; per-channel delivery outcomes already live in `notification_deliveries` (0067), so asking the inbox how something was delivered was a category error — which is why the answer was a constant. The backfill is right here, unlike `0079`'s refusal to rewrite health samples: those were observations, this was a constant known to be wrong for every row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | P1       |
| ~~BUG-050~~ | Web push                                   | Displayed on the device                      | **CLOSED #165** — `public/push-sw.js` imported into the generated SW. Unproven on a real device (❓-007)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —        |
| ~~BUG-051~~ | `shift_templates`                          | Read by the builder, or removed              | 🟢 **Removed, 2026-08-31 (`0096`).** The row offered two closes and this takes the second, deliberately. Building a reader is speculative — no user has asked, nothing in the design references it — and it would largely duplicate what works: `shift_types` carries the name, colour and default times, and the grid places one on a cell. A template's extra content (a location, a department, required skills) is a **pre-filled placement**, which is a genuinely different feature and should be designed as one rather than resurrected from a table that happened to survive `0002`. Zero rows on production, never written by any code path in the product's life. What it cost while it stayed: a row in the schema docs, an entry in every organisation export, four foreign keys and a set of grants to keep correct, and a place in the mental model of everyone who read the schema and assumed it meant something.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ~~BUG-052~~ | `STRIPE_MODE`                              | Fails closed                                 | **CLOSED #163** — unset is a 503 naming the secret, never assumed live                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | —        |
| ~~BUG-053~~ | Pricing page                               | Reads `plans`                                | 🟢 **Closed 2026-08-31 — by making the duplication checkable, not by removing it.** `PLANS` has to be hardcoded: the marketing page is unauthenticated and `plans` carries an RLS policy requiring a signed-in user (`0023`), so it cannot be fetched at render time. What was missing was anything that noticed when the two disagreed — a price changed in the migration and not in `marketing.ts` leaves a page quoting a number the customer will not be charged, which ends up as a complaint rather than a bug report. `src/lib/marketing.test.ts` now reads the seeded rows out of the migration and compares name, price and summary. It parses SQL with a regular expression, which is not elegant; the alternative is a duplicated constant nothing checks, and a brittle test that fails loudly beats a comment asking people to remember. It also asserts it found all four plans first, so the regex cannot silently match nothing and pass vacuously.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ~~BUG-054~~ | Audit screen                               | Labels the actions written                   | ✅ **Closed earlier this session, struck 2026-08-30.** `src/lib/auditActions.ts` holds 41 labels and `SettingsAuditPage` renders through `formatAuditAction`, which falls back to a readable phrase rather than a raw key. The row outlived the fix.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | —        |
| ~~BUG-055~~ | Own activity                               | A user sees their own actions                | ✅ **Closed by `0071`, struck 2026-08-30.** The policy tested `visibility = 'org'`, an equality, so every row written as `'both'` was readable by platform staff and nobody else; it now tests `<> 'platform_only'`. What is still thin is the _screen_, because most rows carry no `actor_user_id` — that is BUG-063, which `0071` deliberately did not paper over.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | —        |
| ~~BUG-056~~ | Data-residency claim                       | Accurate                                     | ✅ **Closed 2026-08-29/30, struck 2026-08-30.** `DATA_LIFECYCLE.md` §2 was corrected to name OpenRouter and Stripe as US processors, and the list is now published at `/legal/trust` (GAP-014). Writing that page then found the AI was also sending leave `type`, so a named person's sickness dates were leaving the UK/EU; that field is no longer selected.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | —        |
| ~~BUG-057~~ | Connector catalogue                        | Reflects real state                          | ✅ **Closed 2026-08-30.** `0073` adds a `planned` status and sets all eight to it with `available = false`. They were seeded "operational", "degraded" and "beta" — describing software never written — and because `connect_integration` checks `available`, the database would have let an owner connect Sage Payroll and sync nothing indefinitely. It now refuses. Customer-facing `/app/settings/integrations` was never affected: it is SMTP only, and that is real.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | P2       |
| ~~BUG-058~~ | AI announcement                            | Grounded like the rota task                  | 🟡 **Closed 2026-08-30.** `grounding.ts` checks the draft against the facts the model was given. **Dates** are decidable, so an ungrounded one is refused outright rather than warned about — the composer pre-fills the fields, and a caution printed beside text already in the box is read after it is posted. A date is grounded if it falls in the rota period _or_ appears in the manager's own prompt: "the Christmas rota closes on 12 December" is a manager stating a fact, and refusing it would make the feature useless outside the current week. **Names** are only decidable enough to warn — English capitalises plenty of words that are not names — so a name-shaped word matching nobody is returned as `unverified` and shown in the composer. **Numbers are deliberately not checked**: times, durations and ordinals all read as numeric claims, and a check that cries wolf trains managers to ignore it. That half stays open as CAP-071 🟡.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| ~~BUG-059~~ | Console figures                            | Real data                                    | ✅ **Closed 2026-08-30.** File deleted. Support-access refusals became a real count of `support_access_allowed = false`; the Services list and both health panels now read `platform_health_summary`. An invented ten-service list (two of them services that do not exist), per-service hourly history strips, an error rate, an auth-success rate and a twelve-point latency trend chart were deleted rather than relabelled — the shape of a trend line is itself a claim. Verified on all three screens through `/admin-preview`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | P2       |
| ~~BUG-060~~ | Support CSAT                               | Collectable                                  | ✅ **Closed 2026-08-30.** `/app/help` gains "Your requests": the requester's own cases, the non-internal thread (excluded by the RLS policy itself, not by a client filter), and a 1-5 rating on resolved ones. Rating needed somewhere to rate _from_, so this is also the first place a requester sees what happened after they asked — half of CAP-080. `0077` gives the RPC its own out-of-range message; it used to fall through to the column CHECK and surface as `support_cases_csat_check`. `supabase/tests/database/support_csat.test.sql`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | P2       |
| ~~BUG-061~~ | Concurrent approval                        | Second writer is rejected                    | ✅ **Closed 2026-08-30.** All three review paths — leave, swaps, overtime — now filter the UPDATE on the undecided state, so of two managers racing, one writes and the other gets `null` and a reload rather than silently replacing the recorded decision. Overtime's Withdraw carries the same guard from the staff side. `src/services/reviewConcurrency.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | P2       |
| ~~BUG-062~~ | Seat usage vs enforcement                  | Console reports what is enforced             | ✅ **Closed 2026-08-30, and added in the same change — it was a dangling id.** `0070` deferred this to "BUG-062" but no row was ever created, so a merged migration cited a register entry that did not exist (the `docs/audit01.md` failure mode, caught early). The defect: `0070` enforces `seat_limit` on active `staff_profiles`, the customer's billing screen counts the same, and the console's Usage bar divided **memberships** by the limit. `staff_profiles.user_id` is nullable and most rostered staff never sign in, so a tenant at its cap could read "Usage 20%" while the database refused its next hire — support reading a bar with room on it. `0078` adds `platform_staff_counts()`, aggregate-only for the reason `0054` gives. Also fixed: an uncapped plan read 0%, indistinguishable from an empty tenant. `supabase/migrations/0078_platform_staff_counts.sql`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | P2       |
| ~~BUG-063~~ | Own-activity screen is empty               | Shows what the user did                      | 🟢 **Closed 2026-08-31 — and the diagnosis in this row was wrong.** It said the fix was "for the trigger bodies to record who acted". They already do: `audit_write` records `auth.uid()`, and production has 40 rows carrying a real actor to prove it. It also implied a widening was needed; `audit_logs_select` has read `... or actor_user_id = auth.uid()` since `0016`, verified against the live policy. What the 326 actorless rows actually are: 238 written through a service-role path and correctly labelled `metadata.via = 'service_role'`, and 88 written before that labelling existed. Neither belongs to anybody's personal activity, and neither can be backfilled — `audit_logs` is append-only by trigger, which is the point of it. **The real defect was on the screen**, which told users their activity was hidden by an "owner-only" policy and that only anonymisation was ever recorded. Both untrue; both corrected, along with an empty state that now points at the org-wide trail instead of implying a clean record.                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| ~~BUG-064~~ | Three ungated entitlements                 | Each gated, or removed from the plan tables  | 🟢 **Decided one at a time, 2026-08-31.** **`gps_clock_in` is not an entitlement** — every plan had it and the pricing page sells it as a Starter line, so a gate could never refuse anything; `0090` removes it from `plans.features`, which makes `org_has_feature` return false for it. That is deliberate: a name resolving to "false" is a better trap than one resolving to "true for everyone", because it fails visibly if somebody wires it up. **`beta_integrations` is retired** — "unreleased payroll and HR connectors", of which none exist; the only tenant-reachable integration is their own SMTP, and `0073` marked every platform connector `planned`. **`advanced_reporting` now gates the Reports screen**, which is the one of the three with a decision behind it — and the decision was already made on the pricing page: Starter's feature list has no reporting line, Professional's says "Reports across every site", and `plans.features` has agreed since `0030`. Nothing read it until now. **It is client-side, and the register says so rather than dressing it up:** every row on that screen is computed in the browser from the org's own `clock_events` and `staff_profiles`, which RLS grants them because the records are theirs. There is no server endpoint to refuse, and hiding a customer's own data from them would be a worse product. Packaging, not a control — unlike `seat_limit`/`location_limit` (`0070`, writes) and `ai_rota_assistant` (`0074`, spends money). |
| ~~BUG-066~~ | Support reply attribution                  | A reply from support reads as from support   | 🟢 **Closed 2026-08-31.** `/app/help` tested `author_side === 'agent'`. The column’s CHECK (`0024`) allows `'customer'` and `'platform'` and nothing else, so the branch could never fire — every reply from the support team was labelled with **that person’s own full name**, taken from `profiles.full_name`. Two things wrong at once: a customer was shown a staff member’s name they never asked for, and nothing on the screen said the reply came from RotaFlow at all. A comparison against a value a column cannot hold is invisible to TypeScript — `author_side` is `string` in the generated types — which is why it survived.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| ~~BUG-067~~ | Sub-processor list                         | Every row cites evidence that exists         | 🟢 **Closed 2026-08-31.** `/legal/trust` published **Inngest** as a sub-processor receiving recipient user ids and notification bodies, citing `src/services/notificationDispatchService.ts`. Nothing has been sent to Inngest since `0087` and that file was deleted with the path. So a procurement document — the one page whose whole claim is that each row can be checked — named a processor that receives nothing and proved it with a path a reviewer could falsify in one `ls`. Over-declaring errs in the customer’s favour, which is exactly why nobody noticed. Row removed, `SUB_PROCESSORS_REVIEWED` moved to 31 August.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| ~~BUG-068~~ | Cookie notice contradicted by the app      | The published storage claim is true          | 🟢 **Closed 2026-09-04.** `/legal/cookies` and the Privacy summary both published "no analytics, no tracking, and no third-party script on this site" while `src/lib/sentry.ts` added `browserTracingIntegration()` at a 20% sample and `replayIntegration()` at `replaysOnErrorSampleRate: 1.0`, started from `src/main.tsx` before the first render, on every route including the ones making the claim. Traced URLs carry organisation and staff ids in the path; a replay is a masked recording of a session. Nobody was asked. This is the failure mode BUG-067 was about pointed the other way: that page over-declared a processor, this one under-declared what the product did. Replay and tracing removed, crash reporting put behind consent (CAP-058), the sub-processor row rewritten.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| ~~BUG-069~~ | "GDPR compliant" badge on the auth screens | No unverifiable compliance claim on the site | 🟢 **Closed 2026-09-04.** `AuthTrustStrip.tsx` rendered a bare **GDPR compliant** badge under every card on `/login` and `/signup`, and `StepChoosePlan.tsx` repeated it inside the signup funnel. It is the exact claim `src/lib/marketing.ts:151-159` records as _deleted_ from the adjacent feature list for being unverifiable, under a rule `docs/BRAND.md` states outright. The file's own comment defended it as describing "the actual architecture" — a compliance claim is not an architecture. Replaced with two facts a reader can falsify: data held in the EU, tenant isolation in the database.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| ~~BUG-070~~ | Query strings reaching Sentry              | An error report carries no filter values     | 🟢 **Closed 2026-09-04, and only runtime verification found it.** Scrubbing was added to `beforeBreadcrumb`, the unit tests passed, and a real envelope from a production build still carried `?secret=…` in full — because Sentry writes `event.request.url` from `window.location.href` and never routes it through the breadcrumb hook. That field is the one a password-reset or magic-link token rides in on. Fixed with `beforeSend`, re-probed against `vite preview`, and guarded by `src/lib/sentry.test.ts`. The lesson is the one this register keeps relearning: a test that asserts a hook was _configured_ proves nothing about what leaves the browser.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |

| ~~BUG-071~~ | Swap approval and reassignment | Both commit, or neither does | 🟢 **Closed 2026-09-05 (`0123`).** `decideShiftSwap` made two round trips: PATCH the swap to `approved`, then call `apply_swap_reassignment`. Anything between them left the swap approved with the shift still on the original person, and `enqueue_swap_reviewed_notification` had already told the requester it went through. The screen asked the manager to move it by hand, which is a recovery procedure, not correctness — nothing made them, and nothing noticed if they did not. `decide_shift_swap` now does the lock, the authorisation, the decision, the reassignment and the audit in one transaction. Evidence: `supabase/tests/database/swap_decision_atomicity.test.sql` (16 assertions) and `src/services/swapDecision.test.ts` (9). | P1 |
| ~~BUG-072~~ | Replaying an approved swap | A spent swap changes nothing | 🟢 **Closed 2026-09-05 (`0123`).** `apply_swap_reassignment` took no row lock, kept no record that it had run, and updated the shift by id with no predicate on who held it — so replaying an old A-to-B approval reverted a later legitimate B-to-C transfer. It also did not refuse an archived rota, which `0061` otherwise treats as history. `shift_swaps.applied_at` now spends the swap, and both entry points check the current assignee and the rota status. Evidence: the same pgTAP file, sections 2 and 3. | P1 |
| ~~BUG-073~~ | Two managers approving one timesheet | One decision per person per period | 🟢 **Closed 2026-09-05 (`0124`).** `timesheets` had carried no unique key on (org, staff, period) since `0002`, and the client compensated with a read-then-insert-or-update. Two managers both read "no row yet" and both inserted: the audit reproduced two `approved` rows for one week saying 480 and 420 minutes, no error raised, and payroll read whichever came back first. A batch could also half-apply, because the inserts were one statement and the updates a loop of separate ones. `timesheets_period_unique` plus `approve_timesheets()` fixes both. Verified against the live project first: 0 timesheets, 0 duplicate groups, so there is nothing to reconcile. Evidence: `supabase/tests/database/timesheet_uniqueness.test.sql` (11 assertions). | P1 |
| ~~BUG-074~~ | Attendance across a period boundary | A night shift is paid, once | 🟢 **Closed 2026-09-05.** Clock reads were bounded exactly by the reporting window, which broke the pairing at both edges. The period a night shift ENDED in got the `out` without its `in`, and `pairClockEvents` correctly ignores an orphan — so it contributed nothing. The period it STARTED in got the `in` without its `out`, so the segment was closed against `now`: a report run three days later paid three days, with no review flag, because as far as the function could tell the person was still clocked in. That second half is worse than the audit reported. Reads now take 24 hours of context either side and `segmentsStartingWithin` keeps what the period owns, under the start-date rule `listShiftsForPeriod` already applied. Evidence: `src/lib/hoursBoundary.test.ts` (9 assertions, incl. a fall-back DST night). | P1 |
| ~~BUG-075~~ | Reports above the API row cap | Complete, or an error | 🟢 **Closed 2026-09-05.** Clock, shift and rota reads took the single page PostgREST returned and treated it as the whole answer. A response of exactly `max_rows` is indistinguishable from a complete one, so a large organisation's month was simply short — no error, nothing on screen. `fetchAllPages` pages every one of them with `id` as a tie-breaker, and throws rather than truncating past a guard. Raising `max_rows` was rejected: it moves the cliff rather than removing it. Evidence: `src/lib/pagination.test.ts` (5 assertions). NOT load-tested at scale — see GAP-071. | P1 |
| ~~BUG-076~~ | "Invitations sent" after a failed send | The message matches what happened | 🟢 **Closed 2026-09-05.** `sendInviteEmail` returns `{ sent: false, reason }` for an HTTP failure rather than throwing, and onboarding discarded the result, so a 503 produced the same green toast as a successful send. The invitation was real, which is why nobody noticed: staff simply never heard, and the owner had no reason to chase it. Per-row delivery state now distinguishes created, emailed and not-emailed, and the sentence is chosen in `src/lib/inviteDelivery.ts` so a test can hold it. The three other invite paths already handled the result correctly. Evidence: `src/lib/inviteDelivery.test.ts` (6 assertions). | P1 |
| ~~BUG-077~~ | Offline work after an online restart | Queued writes send themselves | 🟢 **Closed 2026-09-05.** Auto-flush fired only on an offline-to-online transition while the hook was mounted, and the hook was mounted only on the clock, leave and swap screens. The ordinary case — clock in with no signal, close the app, reopen somewhere with signal — never flushed at all, because the event happened while the app was closed. `OfflineQueueDrain` mounts at `AppShell`, so replay is a property of being signed in; it also flushes on foreground, and a Web Lock stops two tabs burning the same retry budget. | P1 |
| ~~BUG-078~~ | Cache teardown on an identity change | No screen shows the last user's data | 🟢 **Closed 2026-09-05.** `clearTenantState` ran only in the `signOut` wrapper — the one path a user takes deliberately. An expired token, a session revoked from another device, or a second account signing in on the same browser all went through `onAuthStateChange`, which only called `setSession`. On a ward tablet the next person could be served the last one's cached REST responses. Teardown is now keyed on the user id changing, not on an event name, so a session _replacement_ is caught too. The queued outbox is deliberately untouched. NOT reproduced in a browser — see GAP-072. | P1 |
| ~~BUG-079~~ | Stripe replay and mode scoping | Duplicates converge; test cannot touch live | 🟢 **Closed 2026-09-05 (`0125`).** The webhook kept no record of which events it had applied, and `invoice.payment_failed` wrote `past_due` and incremented `attempts` unconditionally. The audit replayed `invoice.paid`, then an OLDER failure, then that failure again: `past_due`, `attempts = 2`, `paid_at` still set — a paid customer suspended by a redelivery, with a dunning count inflated by Stripe retrying its own webhook rather than by the card. Separately the subscription update filtered on `org_id` alone while both modes deliver to one URL. `billing_events` now keys a receipt on provider, mode and event id and is completed only after effects commit; `decideInvoiceFailure` refuses to regress a paid invoice and prefers Stripe's own `attempt_count`; `subscriptionScope` pins the write to one subscription in one mode and declines rather than widening. Evidence: `supabase/functions/stripe-webhook/reconcile.test.ts` (14) and `supabase/tests/database/billing_event_idempotency.test.sql` (10). NO real Stripe call was made — see GAP-073. | P1 |
| ~~BUG-080~~ | Invited department and location | The assignment survives acceptance | 🟢 **Closed 2026-09-05 (`0126`).** "Invite your team" offered both from the day it was built and stored neither: `create_invite` took org, email and role, and `invites` had no column for either. A manager assigned twenty people to sites during onboarding and every one joined unassigned. The only record of this was a code comment. Both are now ids, validated against the inviting organisation inside the SECURITY DEFINER function, and applied at acceptance. The department list was hardcoded Care/Nursing/Support/Admin — belonging to no organisation and saved nowhere — and now reads the org's real departments, hiding itself when there are none, which is every organisation at that point in onboarding. | P2 |
| ~~BUG-081~~ | Retrying the About step | No duplicate locations | 🟢 **Closed 2026-09-05.** The step created every named location unconditionally, so a failure on the third left the first two behind and pressing Continue again duplicated them. It now reads what exists first. | P2 |

**Carried forward, still open** from the 2026-08-23 release audit: BUG-010, 011, 012, 013, 014,
015, 024, 029, 030, 031, 032, plus the `auth.users` half of GDPR erasure.

These are **`docs/QA-AUDIT-REPORT.md`'s numbering, not this register's**, and they deliberately
have no rows here — the two sequences collide (this file's BUG-010 would be a different defect
from the audit's). An earlier version of this paragraph said they had "no home in the repo until
this file", which read as a claim that they had been given rows. They have not. The audit report
is a dated record of testing performed and remains the place they live — it was rewritten on
2026-09-02 by the website and PWA audit, which kept the 2026-08-14 document whole as its
Appendix A precisely so this pointer stays true; anything from it that warranted ongoing
tracking was re-raised here under this register's own numbering.

## §7 Gap register

| ID          | Gap                                                                                           | Why it matters                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | Priority |
| ----------- | --------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| GAP-001     | No production backups                                                                         | 🔴 **NOT mitigated — measured 2026-08-31, and this row said otherwise.** The workflow has now actually run, on its 02:40 schedule, and **failed**: `Backups are not configured. Missing secret(s): SUPABASE_DB_URL BACKUP_PASSPHRASE. Until these exist there is no backup of production at all.` `gh secret list` returns exactly one secret for this repository, `OPENROUTER_API_KEY`. So **zero backups of production have ever been taken**, and #259 rehearsed restoring a file that is never produced. This row led with 🟡 "Mitigated" and put the condition sixty words later, which is how a reader scanning glyphs concludes the largest risk on the list is handled. It is not; only the mechanism is built. `.github/workflows/backup.yml` takes a nightly `pg_dump`, encrypted with a passphrase held only in Actions secrets, kept 90 days. Encrypted because this repository is **public** and artifacts on a public repo are downloadable by anyone who can read it — the dump is every staff record, every GPS-stamped clock-in and the audit log. **It is inert until two secrets exist** (`SUPABASE_DB_URL`, `BACKUP_PASSPHRASE`) and fails loudly saying so. What it does not give is recovery to a moment: a bad migration at 14:00 costs everything since last night. That needs PITR, which is a **paid Supabase add-on on top of Pro, and the organisation is on the free plan** — a recurring charge, and the owner's decision. Still true: **no restore has ever been tested** (❓-005).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | **P0**   |
| ~~GAP-002~~ | No migration safety gate                                                                      | ✅ **Closed 2026-08-30.** `npm run check:migrations` runs in `verify` and flags nine classes of statement that are irreversible or that silently widen access — `drop table`, `drop column`, `truncate`, `DELETE`/`UPDATE` with no `WHERE`, `disable row level security`, a grant to `anon`, a column type change, a dropped policy with no replacement, and `security definer` without a pinned `search_path`. It does not refuse them; it requires `-- SAFETY(<rule>): <why>` in the migration, so the reasoning ends up beside the SQL rather than in a review comment. Statement-anchored after an unanchored draft called `0075` — the migration that took TRUNCATE **away** — a destructive change: verified to fire on all nine and to be silent on all ten migrations written today.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | P0       |
| ~~GAP-003~~ | `e2e` and `db-tests` are not required checks                                                  | ✅ **Closed 2026-08-30.** Both are now required on `main` alongside `verify`. The evidence is this session: `db-tests` caught two real migration defects and `e2e` caught a third, and until now a pull request could have merged with all three failing. `enforce_admins` is still off, so a repository admin can still bypass — that is a deliberate separate decision, tracked as CAP-097.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | —        |
| ~~GAP-004~~ | No delivery tracking table                                                                    | **CLOSED #168** — `notification_deliveries` (`0067`). The manager-facing screen that reads it is not built yet                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | —        |
| ~~GAP-005~~ | Invites send no email                                                                         | ✅ **Closed 2026-08-30.** `send-invite` shipped in `0058` and #170 wired two of the three invite paths to it — the register recorded that as closed while **both sales-led paths still asked an administrator to copy a link and send it by hand**, to a prospect who has never seen the product. Creating an organisation and re-inviting its owner now email automatically. The first needed `0084`: `admin_create_organisation_with_invite` did not return the invite **id**, which `send-invite` requires because it looks the invitation up by id before verifying the token hash — a guarantee worth keeping rather than relaxing to a hash lookup. Best effort on top of a durable invite in both, so a refused send is reported and the copyable link stays.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | P1       |
| ~~GAP-006~~ | Minimum cover is client-side only                                                             | ✅ **Closed 2026-08-30.** `0080` moves the check into `publish_rota`, so a direct POST to the RPC can no longer publish an understaffed rota. It does not invent a policy — the Rota Builder has always treated a shortfall as `critical` and blocked publishing; this makes the same rule hold for a caller who never rendered the button. The risk was the two disagreeing, so the SQL reproduces `rotaInsights.ts` exactly (distinct assigned staff, by the day the shift **starts** in the location's timezone, cancelled excluded, past days skipped, `min_staff = 0` means no minimum) and nine pgTAP assertions pin each of those — one of which caught a real scoping bug in the first version, where every rule in the organisation was joined so a shortfall at one site would have blocked another site's rota. The escape hatch is lowering that day's minimum in Settings → Policies, deliberately not a force flag. Also fixed: the client flattened every publish failure into "Please try again", which is wrong advice for a rule no retry can satisfy.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | P1       |
| ~~GAP-007~~ | No amendment diff                                                                             | ✅ **Closed 2026-08-30.** `0083` adds `rota_amendment_changes()` and rewrites the publish trigger so an amendment sends **one message per affected person, saying what changed for them** — "Tue 03 Mar: was 09:00-17:00, now 14:00-22:00". Two defects, and the second was the worse: it said nothing about what changed, and it went to **everyone holding a shift**, so a manager fixing one Tuesday night paged the whole roster. Matching is by VALUE because `begin_rota_revision` copies shifts without their ids; it reports a shift present in one version and not the other and never claims to know a shift "moved". An amendment that changes nobody's hours now enqueues nothing at all. `supabase/tests/database/rota_amendment_diff.test.sql`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | P1       |
| ~~GAP-008~~ | Entitlements enforced nowhere                                                                 | ✅ **Closed 2026-08-30.** No longer true in either place. `0070` enforces `seat_limit` and `location_limit` with BEFORE INSERT triggers, so a direct PostgREST call meets them too; `0074` plus the AI Edge Function enforce `ai_rota_assistant`, which is the entitlement that costs money per use. Three entitlements are still ungated and that is a deliberate, separately tracked decision — BUG-064, not this row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | P2       |
| ~~GAP-009~~ | No rate limiting                                                                              | 🟡 **The three things this row names are closed, 2026-08-30.** `0085` adds a shared limiter, `0086` wires invitations. Org creation 5/hour per user (the platform-admin path is exempt — it is already role-gated, and a limit meant for signup should not stop somebody onboarding customers for a living); invitations 60/hour per **organisation**, because the resource being protected is our sending domain and it does not care which manager sent the mail; the AI 30/hour per user **and** `max_tokens: 2000`, which did not exist, so a single call could generate until the model stopped and bill for all of it. The subject-taking limiter is revoked from every client role and clients get one that derives the subject from `auth.uid()` — a limiter that lets a caller name its subject is a denial-of-service tool. **Auth endpoints are not covered:** login, signup and reset are GoTrue's own, configured in the Supabase dashboard rather than in this repository, and are recorded as GAP-031.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | P1       |
| ~~GAP-010~~ | No authenticated E2E coverage                                                                 | 🟢 **Both halves closed. The second landed 2026-08-31 (#222).** The first half added 26 authenticated screens to the axe gate — but through `-preview` routes, which mount the real components against fixtures with no session, so it proved a screen renders and nothing more. `e2e-authenticated` starts a local Supabase stack, signs a new customer up and creates an organisation: a real GoTrue session, `handle_new_user`, `create_organisation`, and `on_org_created` bootstrapping the owner membership — the tenant-bootstrap RLS problem this schema is built around. Landing on step 2 means the row was written **and** the membership came back through RLS. It is also the exact failure `docs/QA-AUDIT-REPORT.md` recorded ("a completely new customer cannot get past step 1"), which nothing in CI could previously have caught recurring. The spec **skips** unless `E2E_LIVE_SUPABASE=1`: run against a developer's `.env` it would point at production and create real organisations. Deliberately **not** a required check yet — it drives a browser against a Docker stack, and it should earn that by being reliable rather than by being declared.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | P2       |
| ~~GAP-030~~ | Colour-contrast debt across the authenticated app                                             | 🟢 **Light mode is at zero, 2026-08-30 — 172 nodes to 0, verified by the gate that measured them.** Not one blanket sweep of the 344 `text-{status}` call sites, but four causes found by dumping the failing nodes: (1) status colours used as text, moved onto their `-ink` tokens with `dark:` keeping `DEFAULT`, the pairing `Badge.tsx` settled on; (2) **`Callout`'s `[&_p]:text-content-muted` alone was 60 of the 172** — every `<p>`, `<code>` and `<strong>` inside a callout inherited muted grey at 4.26:1 on a tinted panel. Muted grey is designed against white; a tinted panel has no room for it, and the semibold title already carries the hierarchy; (3) the clock-in CTA fill darkened #068D41 → #05833C, because white on it was 4.29:1 on the one control that screen exists for; (4) two "disabled" `<span>`s became real `<button disabled>` — `opacity-60` over muted grey is 2.32:1 and axe cannot apply the disabled exemption to a span, which also means a screen reader was never told the action was unavailable. The budget is now **0**, so light-mode debt cannot come back. Dark mode is a separate finding: GAP-032.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P2       |
| ~~GAP-011~~ | No scheduled health probe                                                                     | ✅ **Closed 2026-08-30.** `0076` adds `probe_platform_health()` on a five-minute `pg_cron` schedule: the database directly, Auth and REST over `pg_net`. Records **status only** — `net._http_response` has no duration column and `created - sent_at` includes the pg_net worker's polling interval, so a latency figure from it would be mostly scheduler noise. Uptime is now trustworthy; latency still comes from console probes and is labelled as such. Also added: 90-day pruning, which nothing did. `supabase/migrations/0076_scheduled_health_probe.sql`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P2       |
| ~~BUG-065~~ | REST probe reports a healthy service as degraded                                              | ✅ **Found on production and closed the same day, 2026-08-30.** `0076` and `0075` merged together and interact: `0075` removed `anon`'s table grants, so `GET /rest/v1/` began answering 401, and `0076`'s classifier maps any 4xx to `degraded`. "REST API" therefore read degraded on every tick of a completely healthy platform — seven consecutive samples, all wrong. `0079` classifies REST as **liveness** (only silence or a 5xx is a fault, because an authorisation refusal still proves PostgREST answered) and keeps Auth **strict**, since `/auth/v1/health` returns 200 when well. The distinction lives on the probe row, so a new probe must state which it is. Historical samples are left alone: rewriting monitoring history to flatter a past reading is not a habit to start.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P2       |
| ~~GAP-028~~ | Health page copy contradicts the probe                                                        | ✅ **Closed on merge, 2026-08-30.** Opened by GAP-011 as a merge-order hazard and resolved in the same change that created it: `0076` merged after the BUG-059 branch, so the health page's "samples are only written when an administrator opens this page" and "a scheduled probe is not running yet" were corrected as part of landing it. The page now states that uptime is probed every five minutes and explains why latency still is not.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | P2       |
| ~~GAP-012~~ | Support has no reply path to the customer                                                     | 🟢 **Closed 2026-08-31, in three parts and none of them a new mechanism.** Reading the thread landed with BUG-060; the clock landed with `0110` (CAP-080); and the reply itself needed only a box, because `reply_to_support_case` has been in the schema since `0024`, granted to `authenticated`, deciding for itself that the writer must be the requester or an owner of the case’s organisation and can never write an internal note. Only `/admin` had ever called it. So for two months support could ask a question that the person it was addressed to could read and not answer — the guard was never the missing piece, the door was. Closed on a resolved case as well as an open one, deliberately: "this is not fixed" is the reply that matters most, and it has to arrive before the case closes. **Email ingress is not covered and is not this row** — replying still means opening the app. That is GAP-035.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | P2       |
| ~~GAP-013~~ | No scheduled job for expiry or missed clock-in                                                | 🟢 **Closed 2026-08-31 by `0093`.** Both facts were computed correctly and in the wrong place — on a dashboard render — so the alert existed only while somebody was looking at the screen it appeared on, and nobody is looking at 06:00. A pg_cron job every fifteen minutes now enqueues both into the outbox, which since #210 actually drains. **The dedupe this row named is the whole design:** `notification_outbox.dedupe_key` is unique where present and names the FACT rather than the run — one shift, one document at one expiry date — so a fifteen-minute job does not send the same alert 48 times and teach a manager to mute the channel. Alerts go to owners and managers, never the staff member: a missed clock-in needs somebody who can ring them. The screens keep their live view; this is the part that reaches out.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | P2       |
| ~~GAP-014~~ | DPA, sub-processors, AI notice, security disclosure absent                                    | 🟡 **Three of four closed 2026-08-30.** `/legal/trust` publishes the sub-processor list (one row per processor, each citing the file that proves it), the AI transparency notice and the security disclosure policy, plus RFC 9116 `security.txt`. **The DPA is not drafted** — it is a binding contract and legal advice, which `LegalNotice` already rules out for this repo; what a DPA schedules is exactly the published list, so counsel can take it as input. Found while writing it: the AI prompt was sending **leave `type`** beside real names, and leave types include `sick` — a named person's sickness dates leaving the UK/EU, Article 9 data, for a field no prompt rule reads. Removed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P2       |
| ~~GAP-029~~ | `security.txt` expires 2027-08-30                                                             | 🟢 **Closed 2026-08-31 by taking the second of the two options this row offered.** `npm run check:docs` now reads the `Expires` field and fails the build inside 30 days of it, warning from 90. Put earlier than the weekly audit deliberately: RFC 9116 §2.5.5 says a consumer **MUST ignore** the file once the date passes, so an expired `security.txt` is not a stale document, it is no published security contact at all — and it fails silently, with the only person who finds out being a researcher who then has nowhere to send what they found. Renewing it is a one-line change; the point was never the work, it was that nothing would ever remind anybody. Verified both ways: green at 363 days, red when the date is moved inside 30.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P3       |
| GAP-033     | No CAPTCHA on the auth endpoints                                                              | ⚠️ Opened 2026-08-31 by GAP-031. Supabase's auth rate limits are **per IP**, so they bound one attacker on one address and are close to irrelevant against a distributed credential-stuffing attempt. CAPTCHA is the mitigation Supabase itself names for this, and the project has it available (`security_captcha_enabled`, hCaptcha or Turnstile). It is deliberately **not** a toggle: enabling it server-side without shipping a captcha token from the sign-in, signup and reset forms breaks every sign-in the moment it is flipped. So it is a two-part change needing a provider account and a site key, and the owner's decision on 2026-08-31 was to track it, not build it now.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | P2       |
| GAP-034     | Leaked-password protection is off                                                             | ⚠️ Opened 2026-08-31. `password_hibp_enabled = false`, which Supabase's own security advisor raises as a WARN: new passwords are not checked against HaveIBeenPwned, so a password already in a public breach corpus is accepted. Offered to the owner alongside the minimum-length change and not taken, so it is recorded as a decision rather than left as a silent default. It affects signup and password change only — no existing user would be locked out — and it is one field on the same Management API endpoint the drift check already reads.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | P2       |
| GAP-035     | Support has no email ingress                                                                  | ⚠️ Opened 2026-08-31 when GAP-012 closed around it. A customer can now open a case and reply to one, but only inside the app: mail sent to a support address reaches nobody, and a reply to a notification about a case is lost. This is the half of support that needs an inbound mail route (a provider webhook into an Edge Function, reference parsed from the subject, unmatched mail opening a new case) rather than another screen, so it is sized and priced differently from everything GAP-012 covered and is tracked apart from it rather than left inside a closed row.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P2       |
| GAP-036     | Both scheduled checks are unarmed                                                             | 🔴 **Opened 2026-08-31 by looking at the Actions tab, which nothing had done.** `backup.yml` and `auth-config.yml` are the two checks that read live state no pull request can change, and `CLAUDE.md` says they "fail loudly when their secrets are missing, which is the point — a check that passes when it cannot see anything is worse than none, because it is believed." They did fail loudly. Both, on their first scheduled run. Nobody looked, because **a scheduled workflow fails where no one is standing**: it blocks no merge, marks no pull request red, and appears on no screen anybody opens. Three secrets are missing — `SUPABASE_DB_URL` and `BACKUP_PASSPHRASE` (GAP-001) and `SUPABASE_ACCESS_TOKEN` (GAP-031) — and every one of them is a credential only the owner can mint, which is why this row cannot be closed here. What can be said precisely is the cost: **for the twenty-four hours these have existed, the project has believed it had a nightly encrypted backup and a weekly auth-settings assertion, and has had neither.** **Made visible 2026-08-31**: the `scheduled-checks` job in `ci.yml` reads both workflows' run history through the built-in `GITHUB_TOKEN` and annotates every pull request with which one has never succeeded and what is unprotected without it. It warns rather than fails, deliberately and against this repository's usual instinct — `check:docs` fails on an expiring `security.txt` precisely because a warning nobody must act on is how a date arrives unnoticed. The difference is who can act: that expiry is fixed by editing a file in the pull request that trips it, and these are fixed by minting credentials only the owner holds. Blocking every merge until then would stop all work to protest a decision the person being stopped cannot make. This does not close the row — nothing is backed up until the secrets exist — it stops the failure being invisible.                                               | **P0**   |
| GAP-037     | Nine dependencies are a major version behind                                                  | ⚫ **Deferred with the blockers named, 2026-08-31.** The in-range updates were taken and every gate stayed green; what is left is nine framework majors, each a migration rather than a bump: **react/react-dom 18→19** and `@types/react` with them, **tailwindcss 3→4** (a config-format rewrite that would touch every token in `tailwind.config.ts`), **vite 6→8**, **typescript 5→7**, **jsdom 27→30**, **@vitejs/plugin-react 4→6**, **tailwind-merge 2→3**, and **eslint 9→10**. The last one is blocked outside this project: `eslint-plugin-jsx-a11y`'s own latest release (6.10.2) peers only to eslint 9, so taking eslint 10 means `--legacy-peer-deps` and an unsupported accessibility ruleset — in a codebase whose a11y posture is a stated priority and cost a two-day contrast programme. **Reopens per package**: eslint when jsx-a11y supports 10, the React line when there is a reason beyond currency, and Tailwind 4 only as its own piece of work. Recorded here so "we are behind" is a decision with conditions rather than a thing nobody mentioned.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | P3       |
| GAP-049     | The offline claims are wider than the offline implementation                                  | 🟠 **Opened 2026-09-04 by writing the specification that should have existed first**, `docs/OFFLINE-SPEC.md`. The write queue is genuinely good: three paths queue in IndexedDB with idempotency keys minted before the first attempt, failures are classified rather than retried blindly, and nothing is discarded without telling the user. The reading story is one runtime rule — Supabase `/rest/v1/` NetworkFirst, **50 entries for 5 minutes**, shared across every screen and evicted by an LRU. Three pieces of copy describe that as more than it is: `OfflineBanner.tsx:14` "Showing cached content", `SplashScreen.tsx:82` "Showing your cached rota", `AppBootScreen.tsx:149` "will use what it has cached" — each can appear on a screen with nothing cached at all. Two smaller findings came with it: `public/offline.html` is precached and **never served** (nothing references it; `navigateFallback` is `index.html`), and a cold offline load of the clock screen renders a failure state even though its write path would have worked (`ClockInPage.tsx:189-192`) — the one screen most likely to be opened without signal. The cheap half is narrowing the copy to what the cache guarantees; the expensive half is building the cache the copy implies. `docs/ARCHITECTURE.md` was corrected in the same change: it said swap **responses** queue, and only requests do. **Opened as GAP-038 on 2026-09-04 and renumbered the same day.** 038-048 were already allocated by the audit of 2026-09-02, and those numbers are written into applied migration headers (`0113`-`0115`) and pgTAP files, which are frozen; these two rows were two days old and cited only here, so renumbering them was the side that cost nothing.                                                                                                                                                                                                                                                    | P2       |
| GAP-050     | Nothing exercises offline behaviour in a browser                                              | 🟠 **Opened 2026-09-04.** `src/services/syncQueue.test.ts` is 40-plus cases and covers the restored-network path at module level, and `fake-indexeddb` backs the outbox tests, so the logic is not unproved. What is unproved is the product: `e2e/` contains no `setOffline`, no throttling and no intermittent-connection specification, and nothing tests the service worker, the manifest, installation or the update prompt. Of the nine network conditions the release gate asks for, one is `PASS` and five are `NOT TESTED` (`docs/PWA-RELEASE-GATES.md`). The realistic worst case is the untested one that matters most: a write queued offline and replayed **after the session has expired**. One Playwright specification using `context.setOffline(true)` would close most of this. **Opened as GAP-039 on 2026-09-04 and renumbered the same day.** 038-048 were already allocated by the audit of 2026-09-02, and those numbers are written into applied migration headers (`0113`-`0115`) and pgTAP files, which are frozen; these two rows were two days old and cited only here, so renumbering them was the side that cost nothing.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | P2       |
| GAP-051     | `public/offline.html` was precached and never served                                          | 🟢 **Opened 2026-09-04, closed 2026-09-05 by taking the second of the two options this row offered.** It was in `includeAssets` so every visitor downloaded it, and `navigateFallback: 'index.html'` meant no navigation could reach it. Deleted rather than wired up: the app shell is precached and handles a cold offline start on its own, so a second offline page would be a thing to keep in step for no benefit.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | P2       |
| GAP-052     | A calendar feed token never expires and nothing rate-limits the endpoint                      | 🟠 **Opened 2026-09-04 by reading `0099` against `supabase/functions/calendar-feed`.** The token is revocable and rotatable, and the unbounded lifetime is a documented decision (an expiring calendar silently stops updating). What is not decided: nothing rate-limits the function, it is deployed `--no-verify-jwt`, and the token travels in the query string — so it is in platform request logs, browser history and any referrer, and the feed carries free-text shift notes over a 210-day window. `last_used_at` is written and nothing reads it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | P2       |
| GAP-053     | Platform _reads_ did not distinguish platform role                                            | 🟢 **Opened 2026-09-04, write half closed by `0116`, read half closed 2026-09-05 by `0122`.** `is_platform_admin()` is true for all four roles and was the whole platform-side predicate on eight tables of operational tenant data, so `platform_finance` — documented in `src/lib/platformRoles.ts` as "Subscriptions and billing state only. No operational tenant data" — read the audit log, support cases including `is_internal` messages, incidents, every tenant's integrations, every membership and every profile. This row previously said "not attempted here: rewriting nine read policies without `supabase test db`" is the mistake this register keeps recording, and Docker was unavailable" — the tooling was available this time, so it was attempted with a test. `is_platform_operational()` is `is_platform_admin()` **and** a role test, composed rather than restated so it inherits the `aal2` MFA gate `0102` added; writing the role list into each policy would have quietly dropped that from eight of them. Two of the nine named tables were deliberately left alone and it is worth saying why: `organisations` stays readable by finance because the billing console calls `listAllOrganisations()` and an org's name, plan and status **are** billing state, and `platform_user_auth_facts` **does not exist** in this schema. Splitting `platform_admin` from `platform_support` is a separate product decision and was not attempted. `platform_reads.test.sql` asserts both directions — finance sees none of it, support still does its job — because a test that only proves finance sees nothing passes just as well against a policy that shows nobody anything.                                                                                                                                                                                                                                                                                                 | P1       |
| GAP-054     | Nothing exercises the service worker, manifest or update prompt in a browser                  | 🟠 **Opened 2026-09-04.** Sibling to GAP-050, which covers offline only. `UpdatePrompt` now has unit coverage, but no test installs a second service worker, so the update path — the one that decides whether a user is ever moved off a broken build — is verified by reasoning alone.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | P2       |
| GAP-055     | The public site has no structured data and one shared link preview                            | ⚫ **Opened 2026-09-04, deferred.** `usePageMetadata` sets a per-route title, description and canonical but never `og:image`/`twitter:image`, so every page shares the site card; there is no JSON-LD anywhere; and the manifest declares no `shortcuts`, `screenshots` or `categories`, with no maskable 192 icon, no dark-mode `theme-color` and no `apple-mobile-web-app-title`. All cosmetic-to-discovery, none blocking.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | P3       |
| GAP-056     | Uploaded files survive erasure and organisation deletion                                      | 🟠 **Opened 2026-09-04.** `anonymize_staff_member` (`0111`) deletes the `documents` row and nulls `photo_url`; the file behind `documents.file_url` is untouched, because nothing in this repository manages the store it lives in. `delete_organisation` (`0063`) has the same hole. So an erasure that the register records as complete leaves a photograph or a right-to-work scan reachable at a URL. Both export functions already warn that files are not included, which is the same fact seen from the other side. The Documents dialog now says so at the point somebody adds one, and the Privacy Notice says so where it describes erasure — but a notice admitting a gap is a disclosure, not a fix. Needs an owner decision about the image host before it can close.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | P1       |
| GAP-057     | No self-service subject access or account deletion                                            | 🟡 **Opened 2026-09-04.** A manager can export one person's record (13 datasets, `gdprService.ts`) and an owner can anonymise them, but an individual has no control of their own: the routes under `/app/account` are Profile, Preferences, Security, Connected accounts, Sessions, Tokens and Activity, and none offers an export or a deletion. `auth.users` is never touched by erasure at all (`0011:4-11`), so there is no account deletion in any sense. The request route is email into `gdpr_requests`, which is case-managed and real, and the Privacy Notice now says plainly that there is no button rather than implying there is. Correct as an interim position; not a permanent one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | P2       |
| GAP-058     | Emergency contacts hold third-party data with no rights channel                               | 🟡 **Opened 2026-09-04.** `emergency_contacts` holds a named person's relationship, two phone numbers and a free-text `medical_notes` field — Article 9 health data about somebody who never visited the site, has not been told, and has no way to ask. UK GDPR Article 14 normally requires that person to be informed. The dialog now tells the staff member entering it that these are somebody else's details and to ask them first, which is the only lever the product has. Whether an exemption applies, and how the duty divides between the employer as controller and us as processor, is a question for counsel.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | P2       |
| GAP-059     | No ICO registration, and no answer on an EU representative                                    | 🔴 **Opened 2026-09-04.** RotaFlow is operated by a UK sole trader and is not registered with the Information Commissioner's Office; most UK organisations processing personal data must register and pay the data protection fee. The product is also intended for EEA users, which raises an Article 27 representative question nobody has answered. Neither is a code defect and neither can be closed from this repository — but the Privacy Notice cannot honestly be published as final until both are settled, so the draft names them where the operator's identity is given.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | P1       |
| GAP-060     | No age field, in a product that schedules young workers                                       | 🟡 **Opened 2026-09-04.** There is no `date_of_birth` column anywhere, no age check at sign-up and no minimum age in the terms. The sectors this is built for — hospitality, retail, care — employ people at sixteen and seventeen, and UK working-time rules for young workers differ from those for adults, so the rules engine cannot apply the right ones because it does not know which is which. That is a product gap with a legal edge rather than a pure compliance one.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | P2       |
| GAP-061     | Onboarding step 2 was refused for every new customer                                          | 🟢 **Opened and closed 2026-09-05 by the delivery audit, and it is the reason this database has never held an organisation.** Step 1 creates the org through `create_organisation`, a SECURITY DEFINER function, so it works. Step 2 PATCHes `organisations` over PostgREST and returned **403 `42501` permission denied**, so the screen said "Could not save those details. Please try again." and pressing Continue again did the same thing forever. Not RLS: `organisations_update` is exactly right, `has_org_role(id, ARRAY['owner'])` on both sides. `0017` deliberately replaced the table grant with `update (name, slug, plan, settings)` so an owner could not lift their own suspension, and warned in its own header that "anything omitted here fails with 42501 at the screen that writes it". Then `0023` added `industry`, `country` and `timezone`, and `contact_email`/`contact_phone` followed; the grant never did. **A column added after a column-level grant is not covered by it.** Measured by signing a real user up against a local stack, then confirmed on production with `has_column_privilege` — false there for all five. PostgreSQL's own hint makes it worse: it says `GRANT UPDATE ON public.organisations TO authenticated`, which would fix the symptom and hand every owner `status` and `suspended_at`. `0117` grants the five columns and `org_smtp_settings.verified_at`, which `updateOrgSmtpFields` clears on every edit and `0010` had likewise left out. Three gates each stopped one step short: `rls_invariants` checks policies not privileges, `function_grant_invariants` covers EXECUTE only (`0113`, the same bug one layer down), and `authenticated-loop.spec.ts` asserted step 2 was on screen and stopped at the click before the 403. All three closed: `table_grant_invariants.test.sql` asserts the column list against what `src/` writes and was shown to fail on the real defect, and the e2e now advances to step 3.                     | **P0**   |
| GAP-062     | An owner could put themselves on any plan, free                                               | 🟢 **Opened 2026-09-05 and closed the same day, on the owner's decision.** `plan` has been in the client's UPDATE grant since `0017` and `OnboardingPage.tsx:607` wrote it straight from a radio button, so `PATCH {"plan":"business"}` granted `ai_rota_assistant`, `advanced_reporting`, 200 seats and 20 sites for nothing. Table-level INSERT (`0056`) was the same hole through a different door: an organisation could be **created** on `enterprise`. `0070` reads its limits from this column, so the enforcement and the self-assigned value were the same field. This was the shipped design from when nothing charged for anything, which is why `0017` was right to list it at the time; Stripe has since shipped, so the column now decides what a customer is owed. **`plan` is server-owned as of `0120`:** the browser cannot write it on either statement, every organisation starts on the `starter` default, and `subscriptions_sync_org_plan` sets it from the subscription — `trialing`/`active` entitle, `past_due` entitles only inside the `grace_until` window `0071` maintains, and anything else falls back to `starter` rather than to nothing, so a lapsed customer keeps their rota and loses the paid limits. Onboarding now records the choice as `settings.intended_plan`, which is what Checkout is opened with. **The sync is a trigger, not a line in the webhook, on purpose: edge functions do not deploy on merge and migrations do**, so a fix living in `stripe-webhook` would have been off until somebody remembered to deploy it. Nothing that displays a plan regressed — `SettingsBillingPage` already read `subscription?.plan ?? 'starter'`, and `startCheckout` takes the plan code as an argument rather than reading the column.                                                                                                                                                                                                                        | P1       |
| GAP-063     | The Cookie Notice said "everything", and omitted the four Cache Storage buckets               | 🟢 **Opened and closed 2026-09-05.** `legalFacts.ts` published nine `localStorage`/`sessionStorage`/IndexedDB rows under a heading the pages render as "here is everything it does keep on your device" (`CookiesPage.tsx:41`) and "lists everything stored in your browser" (`PrivacyPage.tsx:90`). The service worker also writes `rotaflow-precache`, `supabase-api` (50 authenticated responses, 5 minutes), `imagekit-media` (200 images including staff photographs, 30 days) and `rotaflow-fonts` (1 year). Two of those hold more personal data than anything that was declared. The list was missed for a structural reason worth keeping: the file's own rule is that it is verified by reading every storage write in `src/`, and the service worker is generated from `vite.config.ts`, outside that sweep. All four are now declared and the rule now says to read `vite.config.ts` too. A transparency claim falsifiable in one DevTools panel is the kind this project treats as serious.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | P1       |
| GAP-064     | The Privacy Notice understated what reaches OpenRouter                                        | 🟢 **Opened and closed 2026-09-05.** `legalFacts.ts` said "first names, job titles, skills and contracted hours go to OpenRouter (US) for that request. **Nothing else does.**" `ai-rota-assistant/index.ts:489` sends `` `${s.first_name} ${s.last_name}` ``, and the same payload carries the organisation name, contract type, every site name, every existing shift with its times, and approved-leave date ranges joinable to those full names by `staffProfileId`. `subprocessors.ts:55` had it right all along, so two published pages disagreed and the one a data subject reads was the understated one. The notice now matches the code and the sub-processor list. The code was left alone deliberately: withholding leave **type** is a real control (GAP-014) and the rest of the payload is what the feature needs, so the defect was the sentence, not the function.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P1       |
| GAP-065     | Any manager could make themselves an owner                                                    | 🟢 **Opened and closed 2026-09-05 by the delivery audit.** Two HTTP requests, no owner involved: `POST /rest/v1/invites {org_id, email: <their own>, role:'owner', token_hash: sha256(<a token they chose>)}`, then `POST /rest/v1/rpc/accept_invite`. Every link verified read-only against production: `invites_write` was `for all` to owner **and manager** with nothing said about the `role` column; `authenticated` held INSERT/UPDATE/DELETE on `invites`; `role` was client-writable and `invites_role_check` permits `'owner'`; `token_hash` was client-writable with no default or trigger, so the attacker knows the token; and `accept_invite` (SECURITY DEFINER) checks only that the invite is live and its email matches the caller, then runs `on conflict (org_id, user_id) do update set role = excluded.role`, which **upgrades the attacker's existing manager membership to owner**. An owner can call `delete_organisation`, and GAP-001 says there is no backup. The rule already existed in `create_invite` — "Only an owner may hand out ownership" — under a comment reading "SECURITY DEFINER bypasses RLS, so the role check must be explicit here". That is the exact mirror of the defect: the function guarded itself against RLS being absent, and the policy never guarded against the function being bypassed. `0118` **removes the write path** rather than duplicating the rule: the client never inserts an invite (`inviteService.ts` uses `create_invite`, and its only direct write is `update {revoked_at}`), so INSERT/UPDATE/DELETE are revoked from `authenticated`, `update (revoked_at)` is granted back, and the policy is narrowed to say the rule too. `invite_write_paths.test.sql` was shown to fail on the real defect: with `0118` reverted the manager's insert succeeds.                                                                                                                                                                           | **P0**   |
| GAP-066     | Two SECURITY DEFINER functions read across tenants                                            | 🟢 **Opened and closed 2026-09-05.** RLS does not apply inside a definer function, so the membership check must be in the body. Measured on production: `rota_amendment_changes` (`0083`) and `render_notification` (`0108`) are `prosecdef = true`, executable by `authenticated`, and contain no membership check at all. `rota_amendment_changes` filters on `r.id = p_rota_id` alone, so a rota UUID from another tenant returns that organisation's staff `user_id`s, shift dates and site names; `render_notification` reads any org's notification wording past its own `notification_templates_select`. Gated only by not knowing a UUID, which is not an authorisation boundary. `0119` revokes EXECUTE rather than editing 77 lines of security-sensitive body: `rota_amendment_changes` has **no caller anywhere**, and `render_notification` is called once by `send-notification` on a `service_role` client, which is unaffected. `definer_functions_check_membership.test.sql` names both and was shown to fail without `0119`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | P1       |
| GAP-067     | Twelve other definer functions were not reviewed                                              | 🟢 **Opened and closed 2026-09-05.** The twelve `SECURITY DEFINER` functions `authenticated` may execute beyond GAP-066 were each read in full, and **every one guards itself**: the platform-console setters (`admin_create_organisation_with_invite`, `publish_platform_announcement`, `issue_invoice`, `set_invoice_status`, `extend_gdpr_request`, `set_gdpr_request_status`, `assign_support_case`) through `has_platform_role`, the calendar-feed pair through `my_staff_profile_id`. Three legitimately use no guard and are allowlisted with a reason each: `preview_invite` (anon by design, gated by possessing the token), `slug_available` (answers only "is this slug taken", and its two-arg overload proves ownership through `memberships` rather than trusting `p_exclude_org_id`), and `notification_delivery_configured` (returns whether three vault secrets exist, never a value). Having read them, the sweep that was withdrawn on the day it was written could be shipped: `definer_functions_check_membership.test.sql` now asserts the whole class against the guard idioms this schema uses. That order matters — an allowlist nobody has checked records that a sweep was silenced, not that it passed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P2       |
| GAP-068     | The owner had no staff profile, and two screens told them to ask their manager                | 🟢 **Opened and closed 2026-09-05.** `handle_new_org` inserted a `memberships` row and stopped; nothing in `public` ever inserted into `staff_profiles`. A staff record is what shifts, leave, availability and clock events attach to, and every "what does the signed-in person see" query runs through `my_staff_profile_id()`. So a brand-new owner's first visit to `/app/clock`, `/app/leave` and `/app/availability` read "You don't have a staff profile in this organisation... **Ask your manager to add you**", addressed to the person who is the manager, with no route to the screen that fixes it — three capabilities marked 🟢 unreachable on day one for every customer. Closed in both halves: `NoStaffProfileNotice` gives anyone who can manage staff the action instead of the instruction, and `0121` extends `handle_new_org` to create the founder's record, linked by their email so `0053`'s trigger treats it as claimed. The name is split from `profiles.full_name` keeping everything after the first space, and falls back to the email local part rather than letting a thin name block the record. On the trigger rather than in the wizard because `admin_create_organisation_with_invite` creates organisations too. The founder counts as one of the plan's seats — 1 of 15 on `starter` — which is correct and was the owner's explicit choice; `plan_limits.test.sql` and `server_side_dispatch.test.sql` were both updated to count it rather than to work around it. Existing organisations are deliberately not backfilled: the three that exist are `is_demo` leftovers, and a backfill would silently consume a seat in each.                                                                                                                                                                                                                                                                                                                                  | P1       |
| GAP-069     | No skip link on any of the three shells                                                       | 🟢 **Opened and closed 2026-09-05.** WCAG 2.2 Level A, 2.4.1 Bypass Blocks. There was no "skip to main content" anywhere in `src/` or `index.html`, and no `<main>` carried an id. A keyboard or screen-reader user landing on an authenticated screen tabbed through the organisation switcher, the search trigger and eleven navigation links before reaching the page they had just navigated to, on **every** navigation. `SkipLink` is now the first focusable element of `AppShell`, `AdminShell` and `MarketingLayout`, and each `<main>` carries `id="main-content"` and `tabIndex={-1}`. The `tabIndex` is the half usually left out: without it Safari and Firefox scroll the viewport and leave focus at the top of the document, so the next Tab returns to the second nav link and the link appears to do nothing. Verified in a browser — the skip link is the first anchor in the DOM, `focus:not-sr-only` makes it visible when focused, and pressing Enter moves **focus**, not just the viewport, into `#main-content` (`e2e/a11y-fixes.spec.ts`).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | P1       |
| GAP-070     | Every admin table scrolled sideways and no keyboard could scroll it                           | 🟢 **Opened and closed 2026-09-05.** WCAG 2.2 Level A, 2.1.1 Keyboard. `DataTable`'s container is `overflow-x-auto` on a plain `div`: a pointer user drags it sideways, a keyboard user cannot scroll it at all, so on a narrow viewport whole columns were unreachable with nothing on screen to suggest anything was missing. It now carries `tabIndex={0}`, `role="region"` and the caption as its accessible name — the pairing the technique calls for, because a bare `tabIndex` adds an unlabelled mystery tab stop. `jsx-a11y/no-noninteractive-tabindex` objects to exactly this fix and is disabled on the line with the reason written next to it; that rule's own `roles` option lists `region`. Covered by `src/components/ui/DataTable.test.tsx` rather than by Playwright, deliberately: the e2e routes render no `DataTable` at all — the admin overview's two tables are hand-rolled — which is how a browser check can pass while the component it was meant to cover goes untested. The wider sweep of the other 26 `overflow-x-auto` containers, including `RotaGrid`'s `min-w-[860px]`, is not done.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P1       |
| ~~GAP-038~~ | The migration history does not grant `authenticated` EXECUTE                                  | 🟢 **Opened and closed 2026-09-02 by `0113`, in the full website and PWA audit.** Rebuild this database from `supabase/migrations` and no signed-in user can read a single row: every RLS policy in `0002` is `using (public.is_org_member(org_id))`, and on a fresh database `authenticated` holds no EXECUTE on `is_org_member`, so the policy raises `permission denied for function` and the row is refused. Measured against `supabase/postgres:17.6.1.143`: `supabase db reset && supabase test db` gave **192 of 364 assertions run, Result: FAIL**; granting EXECUTE on the four helpers restored all 364. Bisected by resetting to each version in turn — present at `0074`, gone at `0075`, which closed the `anon` hole with `revoke ... from public, anon` and took `authenticated` with it, because PUBLIC was the only path by which it ever held the grant. **Nothing in this repository has ever granted it.** Production is unaffected and always was: it carries `pg_default_acl` `postgres/public/f = {postgres=X, authenticated=X, service_role=X}`, so every function is created already granted, while the local image carries `{postgres=X}` alone. So the defect is invisible everywhere it is currently looked for and appears only in a database built somewhere else — which is the database you build when restoring, and GAP-001 says there is nothing to restore from, making this migration history the whole recovery path. It also means `db-tests` has been proving things about a privilege model that is not production's. `0113` restates production's live grant contract as SQL, read from its catalogue: 88 functions to `authenticated`, all 97 to `service_role`, the nine server-only ones left shut. Every statement is a no-op on production. `function_grant_invariants.test.sql` gains both directions and was shown to fail on the real defect. `supabase/config.toml` now pins the Postgres major version, which is what made the drift possible to miss. | P1       |
| ~~GAP-039~~ | The draft/published boundary was only in the browser                                          | 🟢 **Opened and closed 2026-09-02 by `0114`, in the full website and PWA audit.** Every structural rule around publication lives in the database — immutable shifts, no PATCH of `status`, one published rota per scope (`0061`), minimum cover inside `publish_rota` (`0080`, moved there precisely because the browser's copy could be walked around). The rule the product is named for did not: `rotas_select` and `shifts_select` were `using (is_org_member(org_id))` (`0002:490-503`) and the boundary was a `.filter()` applied after the rows had arrived (`shiftService.ts:66-89`). Verified as `authenticated` with a staff member's `sub` claim, which is what PostgREST does: `select name from rotas where status='draft'` returned `UNPUBLISHED DRAFT — pay cut week`, and its shifts came with it. Then the amendment sequence the audit standard sets out — publish, `begin_rota_revision`, move a shift to 23:00 — and the staff member read the new time before anybody published it. Next week's unpublished draft is the most sensitive thing in the schema while it is being written; it is where a manager tries out cutting somebody's hours. `0114` gives staff `published` and `archived` only and leaves owners, managers and live delegates the full view. `archived` is included on purpose: it is unreachable except by having been published (`0080:148-153`) — an abandoned amendment is DELETED, not archived (`0061`) — so excluding it would erase every staff member's own history. `rota_publication_boundary.test.sql` asserts both directions, and was shown to fail on the real defect. All four draft-inclusive client call sites are inside manager-only branches, so no staff screen loses a row.                                                                                                                                                                                                                                                               | P1       |
| ~~GAP-040~~ | Duplicate clock-ins were refused only by a disabled button                                    | 🟢 **Opened and closed 2026-09-02 by `0115`, in the full website and PWA audit.** Nothing on the server stopped a staff member sending two consecutive `in` events. Measured on a local stack as the staff member's own session: three `in` rows in a row, no `out` between them, all three accepted. `0081`'s idempotency indexes do not cover it — they stop the same event being replayed, which is a different thing from two genuine events that should not both exist. The damage is not a crash: `pairClockEvents` absorbs it into a zero-minute segment flagged `missing_clock_out`, on a timesheet a manager approves for payroll, and the audit standard lists duplicate clock-in and "incorrect attendance record" as release blockers. `0115` refuses an `in` when one is already open **and was recorded within the previous five minutes**, exempting owners, managers and any row whose `client_event_id` already exists. Both halves of that narrowness were arrived at by trying something stricter and watching it break a real path: refusing any `in` while a session is open strands somebody who forgot to clock out last night, and the first version also guarded `out` until `clock_event_reported_time.test.sql` showed that `0068`'s clamp reorders an offline `in` to _after_ a genuine `out`, so any ordering rule on `out` can refuse a real one. Guard the fabrication, never the exit — a person must never become unable to clock out. `clockErrorMessage` gives the screen the guard's own words, because "please try again" is advice to repeat the one thing that cannot work.                                                                                                                                                                                                                                                                                                                                                                                         | P1       |
| ~~GAP-041~~ | Leave and swap requests were lost on a transient failure                                      | 🟢 **Opened and closed 2026-09-02 by the full website and PWA audit.** `ClockInPage` has queued a failed-while-"online" write since BUG-046, because `navigator.onLine` says true on a captive portal and on associated-but-dead wifi, which is routine on a ward. `LeavePage:311-320` and `SwapsPage:205-217` queued only when `navigator.onLine` was already **false**: a transient failure the other side of that check showed "Could not submit that request. Please try again." and dropped the work. Neither minted a `client_event_id` before its first attempt either, so a lost response replayed as a **second booking of the same week off**, or a second swap offer, with nothing for `0081`'s unique index to recognise. All three pages now share `sendOrQueue` (`src/lib/queuedWrite.ts`, pure and unit-tested), and both mint the key before the attempt. Also closed here: `cancelLeaveRequest` was the last decision path with no compare-and-set — a withdraw issued while a manager was approving overwrote the approval, leaving a row that is `cancelled` and carries a `reviewed_by`, and the manager never learned the leave they granted had gone. It now guards on `pending`, which refuses nothing the UI offers (`LeaveRowsTable:145` renders Withdraw on a pending row only) and returns null on the race so the screen says what actually happened.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | P1       |
| ~~GAP-042~~ | The offline outbox was not scoped to a user                                                   | 🟢 **Opened and closed 2026-09-02 by the full website and PWA audit.** IndexedDB is scoped to the ORIGIN, not the account, so `rotaflow-outbox` outlives a sign-out — and `clearTenantState` deliberately does not clear it, because destroying an unsent clock-in is the one outcome the queue exists to prevent. On a shared device, which is most of this product's market, the next person to sign in flushed the previous person's writes under their own token and was shown their failures. The asymmetry is what makes it serious: RLS lets a **manager** insert a clock event for anybody in their organisation (`0037`), so a manager signing in on a ward tablet would successfully land a colleague's queued clock-in on their behalf, hours late, with nothing recording that somebody else replayed it; a **staff member** signing in on the same tablet fails that check, `classifyFailure` calls it permanent, and the colleague's clock-in is DESTROYED into the dead-letter store — with a notice on the new person's screen reading "1 action didn't save" about an action they never took, while the person who did take it still believes they are clocked in. Every record now carries the id of the user who queued it, and `flushQueuedWrites`, `listQueuedWrites`, `listDeadLetteredWrites` and `retryDeadLetteredWrite` are all scoped to it. A record with no owner was queued before the field existed and is claimable by whoever is signed in — stranding a clock-in forever is worse than replaying one, and there is no second copy. Four new tests, including the legacy case.                                                                                                                                                                                                                                                                                                                                                                                            | P1       |
| ~~GAP-043~~ | The public site had no sitemap, canonicals or social cards                                    | 🟢 **Opened and closed 2026-09-02 by the full website and PWA audit.** `sitemap.xml` did not exist — and `/sitemap.xml` answered **200 with `index.html`**, because the SPA fallback serves anything that is not a real file, which is the failure `docs/DEPLOYMENT.md` warns about twice. `robots.txt` still carried the scaffold's commented-out `Sitemap: https://yourdomain.com/sitemap.xml`. `index.html` held one `<title>` and one `<meta description>` for all sixteen public pages; there was **no `<link rel="canonical">`, no Open Graph and no Twitter card anywhere in the repository**, so a link to rotaflow.space pasted into Slack, WhatsApp or iMessage rendered as a grey rectangle with a hostname under it. The four auth routes and the 404 set no title at all — a password-reset tab read "RotaFlow — Scheduling certainty for every shift" — and the 404 was a bare `<main>` with no nav, no footer and no way out, on a page every unknown path reaches. Now: `src/lib/publicRoutes.ts` is one list feeding three consumers (a build-time sitemap plugin, `usePageMetadata`, and `navigationTargets.test.ts`, which fails the build if a listed path has no `<Route>` — a sitemap advertising a URL that renders the 404 is worse than no sitemap); `public/og-image.png` is generated from the real `BrandMark` geometry by `scripts/generate-og-image.mjs`; the 404 is `noindex` because the fallback cannot answer 404 itself. Sixteen new e2e assertions, and `/forgot-password` and the 404 joined the axe sweep they had never been in.                                                                                                                                                                                                                                                                                                                                                                                                                                    | P2       |
| ~~GAP-044~~ | The install banner could not be dismissed, and the update check never ran                     | 🟢 **Opened and closed 2026-09-02 by the full website and PWA audit.** The install banner's own doc comment called it "dismissible" and it had no dismiss control: the only ways out were installing the app or navigating somewhere `beforeinstallprompt` had not fired. It also rendered on the marketing pages, asking a first-time visitor to install software for a product they had not signed up for, over the copy meant to persuade them to. It is now confined to `/app` and `/onboarding`, with a close button whose answer lasts 30 days — not forever, so somebody who declined once on a borrowed laptop is still offered it on their own device. Separately, `registerSW` had no `onRegisterError` (a failed registration loses offline support and the update path at once, with no symptom anybody would report) and no periodic `registration.update()`. A browser only checks for a new service worker on navigation, and an installed PWA left open for a week — exactly how a ward tablet is used — may never navigate, so it can run a build days old and never be told. Now polled hourly. `manifest.webmanifest` gains `id` (identity independent of `start_url`, so changing that later does not register as a second app) and `display_override`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | P2       |
| ~~GAP-045~~ | Enter did not submit any of the four auth screens                                             | 🟢 **Opened and closed 2026-09-02 by the full website and PWA audit.** None of `/login`, `/signup`, `/forgot-password` or `/reset-password` contained a `<form>` element — verified by grep: no `<form>`, no `onSubmit`, no `type="submit"`, no key handler in any of the four. Every submit was an `onClick` on a `<Button>`, so typing a password and pressing Enter did nothing at all on the most-used screen in the product. It is not a nicety: the keyboard is how anybody signing in on a laptop finishes, and how a password manager finishes for them. Each now has a real `<form onSubmit>` with `noValidate` (the messages are ours and are wired to the fields; the browser's bubbles would compete). `Button` also now defaults to `type="button"` rather than HTML's `submit` — the moment these screens gained forms, the magic-link button and the show-password toggle sat inside one, and either would have signed the person in. Every existing caller inside a form already stated its own type, so the default changes nothing that works and removes the trap from everything written next. Four e2e assertions, structural rather than behavioural on purpose: a behavioural test would submit real credentials against whatever project the run points at.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P1       |
| ~~GAP-046~~ | A failed contact submit moved focus nowhere, and two auth errors named no field               | 🟢 **Opened and closed 2026-09-03 by the full website and PWA audit.** `ContactPage.onSubmit` focused the first invalid field with `document.querySelector('[aria-invalid="true"]')` in the same handler as `setErrors`. React batches state, so on the FIRST failed submit nothing carries `aria-invalid` yet, the query returns null and focus never moves — somebody using a keyboard or a screen reader is told there are errors and left exactly where they stood; on later submits it finds the previous render's first invalid field, which is worse than not moving. Now chosen from the validation result and focused by id, in the order the fields appear. Separately, `/forgot-password` and `/reset-password` rendered their only error as an unassociated `role="alert"` paragraph at the bottom of the card: announced once, then unreachable from the field it was about. Both now carry `aria-invalid` and `aria-describedby`. Found by the website-audit agent; each verified in the source before being changed.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P2       |
| ~~GAP-047~~ | Six grammar defects in published marketing copy                                               | 🟢 **Opened and closed 2026-09-03 by the full website and PWA audit.** A sentence on `/features` whose second half had no subject and a doubled "and"; a comma splice closing `/solutions`; "Each one checkable" with no verb in the testimonial band; "which eventually it will" inverted on `/about`; a three-comma appositive on `/about` that made the subordinate clause unreadable; and a fragment in `WHY_ROTAFLOW` whose "they" had no antecedent. All six are live text a visitor reads. **The sweep found no American spellings at all** — ten passes over 38 public-facing files, every hit a false positive, and the correct British forms already in place ("colour is never the only signal", "the pay-critical maths", `-ise`/`-isation` throughout). Four strings will look like hits on the next sweep and must not be changed: `autoComplete="organization"` (an HTML spec token — renaming it breaks autofill), the `teamSize` identifier, `theme-color`/`transition-colors` (CSS property names) and the lucide `Check` import.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P3       |
| ~~GAP-048~~ | The site loaded Google Fonts while telling visitors no third party ran on it                  | 🟢 **Opened and closed 2026-09-03 by the full website and PWA audit.** `/legal/cookies` says in bold: "**There are no cookies, no analytics and no advertising.** No third-party script runs on this site", and `legalFacts.ts:123` repeats it. Meanwhile `index.html` loaded a stylesheet from `fonts.googleapis.com` and four files from `fonts.gstatic.com` on every page, so **every visitor's IP address and User-Agent reached Google before they had interacted with anything** — on a site whose `SUB_PROCESSORS` list does not mention Google at all. A German court has fined a site operator for precisely this arrangement. The claim was the thing worth keeping, so the fonts moved rather than the sentence: both families are self-hosted from `/fonts/` (variable, latin + latin-ext only, 176KiB), declared in `src/index.css` with Google's own `unicode-range` values. They are excluded from the precache — that would have been 23% of the budget for something never precached before, since a cross-origin URL cannot be — and served by a same-origin `CacheFirst` rule instead. The CSP drops both origins from `style-src`, `font-src` **and** `connect-src` (the service worker's font fetch is governed by connect-src, which is why they appeared twice). Verified with a headless load: **zero third-party hosts**, both families reporting `loaded`, and the homepage rendering in Inter.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P1       |
| ~~GAP-032~~ | Colour-contrast debt in DARK mode                                                             | 🟢 **Closed 2026-08-30, the same day it was opened — 195 nodes to 0.** Two causes, both invisible until something measured dark mode. (1) `dark:text-{tone}` was never a fix: it is the `DEFAULT`, and the `DEFAULT` is a fill — #3B6FE0 is 3.74:1 on a #111A2E surface, #D94A3A is 4.12:1. (2) An `-ink` class with no `dark:` pairing carried the LIGHT ink into dark mode, where #2F5BC0 reads 2.5:1 — the fix for one theme actively breaking the other. Added `{tone}.ink-dark`, derived by mixing each `DEFAULT` toward white until it clears 5:1 against **every** dark surface in the system, not just the common one: the washes are the binding constraint, and #182848 is the lightest ground any of this text sits on. `warning` needed no lightening at all (6.44:1 already) but has the token anyway, so the rule `text-{tone}-ink dark:text-{tone}-ink-dark` holds with no exception for somebody to apply wrongly. Both budgets are now **0 with no allowance**.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | P2       |
| ~~GAP-031~~ | Auth endpoints have no rate limit we control                                                  | 🟡 **Read at last, 2026-08-31, and they are untouched defaults.** The row said these were dashboard-only and unverifiable from here. Half right: they are not in a migration, but the Management API returns them, and `scripts/check-auth-config.mjs` reads and asserts them — **as a script. The workflow that runs it has never once succeeded**: `auth-config.yml` errored on its first scheduled run with `SUPABASE_ACCESS_TOKEN is not set, so the auth configuration was NOT checked`. The values below were read by hand through the Management API on 2026-08-31; nothing is watching them for drift, and "asserted" was the wrong word for a check with no credential. The live values — **verify 30/hour per IP, token refresh 150/hour per IP, email sent 30/hour project-wide, OTP 30, anonymous 30** — are byte-identical to another project in the same organisation, which is what nobody-ever-looked looks like. Two things worth knowing rather than just recording: the per-IP limits bound one address and do nothing about a distributed attempt, so **CAPTCHA, not a smaller number, is the mitigation for credential stuffing** (GAP-033); and `rate_limit_email_sent` is project-wide across signup, recovery and email change, so onboarding thirty-one people in an hour hits it — invites are unaffected, they go through `send-invite` and our own SMTP. **Changed:** `password_min_length` 6 → 12, with the owner's agreement, and `src/lib/password.ts` now mirrors GoTrue's real rule; its checklist said "8+ characters" and demanded a symbol, so a user could satisfy every tick on screen and still be refused. **Not changed:** leaked-password protection, offered and not taken (GAP-034).                                                                                                                                                                                                                                                                           | P2       |
| ~~GAP-015~~ | Onboarding has no resume path                                                                 | 🟢 **Closed 2026-08-31 by `0094`.** The organisation is created at the end of step 1 — correctly, so an abandoned wizard still leaves a usable workspace — but `/onboarding` bounced anybody with a membership to the dashboard, which made steps 2-4 unreachable the moment step 1 succeeded. A new customer who closed the tab to find their site addresses came back to a workspace with no locations and no route to the screen that adds them. Nothing recorded whether setup had been finished, so the redirect could not tell "done" from "one step in". `onboarding_completed_at` is that fact; the redirect now resumes at step 2 and **fails safe**, treating an unreadable answer as finished because dropping an established owner into a wizard they completed weeks ago is the worse mistake. The resumed wizard says why it is there and hides Back — step 1 creates the organisation, and a Back button re-opening a create form invites a duplicate.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | P2       |
| ~~GAP-027~~ | `notification_deliveries` has no retention policy                                             | 🟢 **Closed 2026-08-31 by `0092`.** The row was right that a policy alone would have done nothing: `enforce_retention` is an if/elsif over data types it knows, and an unknown one takes the `else` branch, logs "no rule implemented" and deletes nothing. So the policy and the branch land together. **12 months, the shortest policy in the table** — attendance is 36, rota history 84 — because those are records of work and this is telemetry about whether a message arrived. Long enough to settle "I was never told about that shift", not long enough to be an archive. `notification_outbox` is covered too, and only **settled** rows are removed: deleting an old `pending` row would silently drop a notification instead of sending it, which is the exact class of silence the outbox exists to end.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | P2       |
| ~~GAP-026~~ | Dispatch is still browser-initiated                                                           | 🟢 **Closed 2026-08-30 by `0087`.** The four remaining `void send(...)` calls are gone. Leave decisions, swap decisions and announcement publication are enqueued by triggers on the transitions that owe them, so the notification and the state change commit together or not at all — closing the tab on the success toast can no longer record a decision nobody is told about. `useInngestDispatch` is deleted. The unread reminder could not be a trigger (pressing it changes no row) and became `remind_announcement_unread`, which also moves the audience and read-set calculation out of the browser: an announcement's reach used to depend on whatever staff list the page had cached. One behaviour change beyond the move — a manager deciding their own request is no longer notified about it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | P1       |
| ~~GAP-016~~ | No per-tenant branding                                                                        | ⚫ **Phase 2 per `CLAUDE.md`, recorded as a decision on 2026-08-31** rather than left reading as an oversight. See CAP-032.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | P3       |
| ~~GAP-017~~ | No MFA                                                                                        | 🟡 **Half closed, 2026-08-31.** Enrolment exists and the platform console enforces it (`0102`, CAP-049). What is still open is the _tenant_ half: an organisation cannot require a second factor of its own staff. That needs a per-org column and the same aal condition inside `is_org_member`, and it should not ship until somebody has run the console requirement in anger — an org-wide MFA requirement locks out every staff member who cannot enrol, and this product's staff use shared ward tablets.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | P3       |
| ~~GAP-018~~ | No SSO or SCIM                                                                                | ⚫ **Phase 2 — and the copy selling it came down on 2026-08-31** (`0101`, `src/lib/marketing.ts`). Also gated outside this repository: Supabase SAML is a paid add-on above the plan this project runs on. See CAP-068.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | P3       |
| ~~GAP-019~~ | No public API                                                                                 | ⚫ **Phase 2 per `CLAUDE.md`.** A public API is a versioning commitment, not an endpoint; this schema still moves weekly. The account tab already tells users there are no tokens. See CAP-065.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | P3       |
| ~~GAP-020~~ | No outbound webhooks                                                                          | ⚫ **Phase 2 per `CLAUDE.md`.** Smaller than it was — `notification_outbox` and its dedupe keys are the delivery machinery — and still a promise to keep somebody else's endpoint fed. See CAP-066.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P3       |
| ~~GAP-021~~ | No payroll integration                                                                        | ⚫ **Phase 2 — and no longer advertised on the pricing page** (`0101`). Real payroll integration is per-provider and belongs to a named customer's payroll run. See CAP-067.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | P3       |
| ~~GAP-022~~ | No CSV import                                                                                 | 🟢 **Closed 2026-08-31 by CAP-084, and struck two days late.** Team → Import shipped in #242 — a hand-written parser for quoted commas, quoted newlines, CRLF and Excel’s BOM, every row shown back with its own problems, an ambiguous date refused rather than guessed, and a second run of the same file caught by email. The register kept the gap open anyway, which is the exact failure §0’s rule exists to prevent: the capability row was added and the gap row it closed was not touched in the same pull request. What it deliberately does not do is create logins or send mail — sixty invitations off a file somebody was still checking is not recoverable.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | P3       |
| ~~GAP-023~~ | No predictive insight                                                                         | ⚫ **Deferred on evidence, not effort, 2026-08-31.** Production holds zero clock events; a prediction fitted to nothing is a confident number with no evidence under it. See CAP-073.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | P3       |
| ~~GAP-024~~ | No customer-facing status page                                                                | ⚫ **Deferred on the same evidence test as CAP-073, recorded 2026-08-31.** Not effort — this is small: `probe_platform_health` already writes real samples every five minutes (`0076`), `incidents.is_public` already exists, and what is missing is an `anon` read policy over public incidents plus one marketing-shell route. The reason to wait is that a status page is a claim made TO somebody, and production holds zero organisations. A page reporting "all systems operational" from a probe against a platform nobody is using is not reassurance, it is an untested green light that will be believed the first time it is wrong. **Reopens at the first paying customer**, and note that shipping it means granting `anon` a read for the first time since `0075` narrowed those privileges to nothing — that grant is the design decision, not the page.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | P3       |
| GAP-071     | High-volume attendance is paginated but never load-tested                                     | 🟡 **Opened 2026-09-05.** `fetchAllPages` makes the reads complete and `src/lib/pagination.test.ts` proves the paging logic, but nothing here has run against a dataset above the API row cap. The audit's proposed acceptance set is 20 locations, 250 staff, 10,000 shifts and 50,000 clock events, recording query count, returned bytes, browser timings and SQL plans. Until that runs, "complete above the cap" is a property of the code and not a measurement. Production holds 0 organisations, so the data has to be synthetic and the environment has to be a scratch one. NOT TESTED, and it is the honest status.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | P2       |
| GAP-072     | Cache teardown on identity change is unproven in a browser                                    | 🟡 **Opened 2026-09-05.** BUG-078 moved teardown onto every auth transition, and the reasoning is sound, but the scenario that matters — warm an authenticated response, revoke the session from another device, sign in as somebody else, read what is on screen and in `caches` — has not been executed. A unit test cannot reach it: the failure lives in the service worker's `supabase-api` cache, not in React state. Needs a production-build Playwright project with real worker control. NOT TESTED.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | P1       |
| GAP-073     | No Stripe TEST-mode verification of the webhook repairs                                       | 🟡 **Opened 2026-09-05.** BUG-079's decision logic is covered by 14 unit assertions and its ledger by 10 pgTAP ones, but nothing has sent a signed event to the deployed function. Signature rejection, a real duplicate delivery, a reordered pair and a crash between receipt and commit all remain unproven end to end. `STRIPE_TEST_SECRET_KEY` is still absent from the project's secrets, so this is BLOCKED on a credential rather than on work. No real payment was attempted and none should be.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P1       |
| GAP-074     | `create_invite` gained a signature that production has not applied                            | 🟡 **Opened 2026-09-05.** `0126` drops `create_invite(uuid, text, text)` and adds a five-argument form. Until that migration merges, a client built from this branch calls a function the live database does not have, and every invitation fails with PGRST202. The drop is unavoidable — a defaulted five-argument overload makes every three-argument call ambiguous — so the client and the migration have to ship together. Recorded here because "merging a migration changes production immediately" is this project's actual deployment model, and the reverse order breaks invitations outright.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P1       |
| ~~GAP-075~~ | Shared layout, state and motion contracts were convention, not components                     | ✅ **Closed 2026-09-05.** A design review at 390px found the failure the desktop-only axe gate cannot see: `Team` rendered as `Tea`. `PageHeader` and `WorkspaceHeader` gave the title block `min-w-0 flex-1`, so it could shrink to nothing and the flex line therefore **never overflowed** — whether a header wrapped depended on how wide its buttons happened to be, and the same header wrapped inside `AppShell` and did not on a preview page with less padding. Both now render one `ui/HeaderBar` that stacks below `lg` (the breakpoint is on the viewport and the rail takes 256px of it, so a 768px window leaves the header ~430px), with `primaryAction` first on a phone. Also closed in the same pass: the clock-in screen was **4,134px tall on a 390px viewport with `Clock In Now` 1,370px down it** — now 2,378px with the action inside the first viewport, secondary panels behind `ui/MobileDisclosure`; the Team directory drew a seven-column table clipped to two with no cue — now labelled person rows below `md` and a keyboard-reachable `ui/ScrollRegion` above it, adopted across 15 bare `overflow-x-auto` wrappers; `rota/PublicationStatus` separates "Draft · not visible to staff" (neutral) from "2 issues block publication" (red, with a link to the panel); `ui/Field`, `ui/IconButton` (44×44) and named motion durations landed with `Button`'s scale transforms finally removed under `prefers-reduced-motion` — the global rule in `index.css` collapses durations, which does not remove a transform. `e2e/responsive-and-motion.spec.ts` (9 assertions on rendered geometry, not class names) is the regression. Contracts written into `docs/DESIGN.md` §4/§6/§7/§8 and `tailwind.config.ts` together. **Not verified:** real assistive technology, real devices, and no user testing — measurement and keyboard checks only.                                                                                                                             | P2       |
| ~~GAP-076~~ | The rota grid could only be operated with a mouse, and lost its column headings when scrolled | ✅ **Closed 2026-09-05.** Two defects in the screen managers spend most of their time on. **(1) No keyboard equivalent for the drag.** dnd-kit's `KeyboardSensor` was registered, which read as coverage and was worse than none: it translates a chip by a fixed pixel step that addresses no particular cell, and its Enter/Space activation fired alongside the chip's own click, so pressing Enter on a shift both opened the editor _and_ started an unaimable drag — verified in the browser before the change. Replaced with `M` on a focused shift, arrows to choose a person and a day, Enter to commit, Escape to cancel; the landing cell is ringed, the target is announced through a polite live region, and focus returns to the chip afterwards. Drag and keyboard now commit through one `moveShiftTo`, so the clash check, the timezone conversion and the 24-hour-shift guard cannot diverge between them. **(2) Nothing was pinned.** Scrolling to Friday scrolled the names away, and 40 rows of a real rota scrolled the dates away. The date row and the staff column are now sticky — which needed the grid to have its own bounded viewport, since `sticky` resolves against the nearest scrollport and there was none. Also in the same pass: the toolbar's filters collapse below `xl` behind one chip carrying the applied count, so the row that was two rows on a laptop is one; and **68 uses of a status FILL token as text** (form errors, menu items, chips, links) were moved onto their `-ink` pairs — all of them inside dialogs, onboarding steps and error branches that the axe gate cannot open, which is why it read 0 while they were there. `e2e/rota-grid.spec.ts` (8 assertions) is the regression. **Not verified:** real screen-reader software, real devices, and no user testing.                                                                                                                                                                         | P2       |
| ~~GAP-025~~ | No vertical configuration                                                                     | ⚫ **The owner's decision, not engineering's — stated 2026-08-31 rather than left looking like a backlog item.** Every other row in this register can be settled by reading the code; this one cannot. Configurable verticals means choosing between one product that fits four industries adequately and a configuration surface that fits each well and doubles the states every future feature has to work in. Care is the primary wedge per `docs/PRD.md`, and the honest cheap move is to keep shipping one generic product until a named customer is lost for a reason that is specifically vertical. **Reopens on that**, or on a decision to sell into one vertical exclusively.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | P3       |

## §8 Hardening register

| ID             | Capability                 | Weakness                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Priority |
| -------------- | -------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------- |
| ~~HARDEN-001~~ | Table grants               | ✅ **Closed 2026-08-30.** `anon` holds nothing in `public` beyond schema usage. Verified inert first against the live catalogue — every policy requires an identity predicate, no table has RLS off, all three views are `security_invoker`. Two things were **not** inert: `anon` held **TRUNCATE**, which RLS does not filter (unreachable only because `anon` is NOLOGIN and PostgREST never issues it), and `pg_default_acl` would have re-granted full CRUD on the next table created, making `0056`'s "a new table gets no privileges" false on the hosted project.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | P2       |
| ~~HARDEN-002~~ | Security predicates        | ✅ **Closed 2026-08-30, and it did not stay closed — reopened and re-closed 2026-08-31 by `0112`.** `PUBLIC` and `anon` execute revoked on every callable function except `preview_invite`, the only thing the logged-out app calls. Found en route: **`touch_org_activity` was an unauthenticated write** — `security definer`, no membership check, so anyone could set any organisation's `last_activity_at`, which is what `tenantHealth.healthBand` and the console's active-tenant tile read. Now guarded by `is_org_member`. The 15 trigger functions keep PUBLIC EXECUTE deliberately: Postgres refuses to call one directly. **The sweep was point-in-time and the line meant to make it permanent could not work.** `0075` ended with `alter default privileges ... revoke all on functions from anon`, but PostgreSQL grants EXECUTE to **PUBLIC** by default, not to `anon` — so that revoked an entry which never existed, and every function created since has been public-executable unless its own migration remembered. Two of the twelve written on 2026-08-31 did not: `leave_days` (`0109`) and `erasure_retained_columns` (`0111`), both carrying `=X/postgres`. Neither leaks anything — date arithmetic and a list of column names — which is precisely why nobody saw it. `0112` revokes them, re-sweeps, restates `preview_invite` as the single exception, and writes the default as `revoke execute on functions from public`. `function_grant_invariants.test.sql` now fails the build on any non-trigger function `anon` or `PUBLIC` can execute, and asserts `preview_invite` still can be — a grant swept away by accident would break the invitation screen for people who cannot report it.                                                                                                                                                                                                                                                                                                                                                                                                                                                             | P2       |
| ~~HARDEN-003~~ | URL fields                 | 🟢 **Closed 2026-08-31 by `0092`.** `DocumentsModal` already checked that a link starts `http(s)://`, and its comment says exactly why: the value is rendered as a real `<a href>`, so an unvalidated scheme is stored XSS, `javascript:` being the case it names. But that check is in a React component and the row is written through PostgREST, which no component sits in front of — this project's own rule is that a browser-only control is not a control. `staff_profiles.photo_url` had no check in either place. Both are now CHECK constraints, added `not valid` (so they bind every future write without a deploy that either deletes data or fails on one bad row) and then validated, which costs nothing here because both tables are empty.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | P2       |
| ~~HARDEN-004~~ | AI function                | 🟢 **The cost half is closed, 2026-08-30.** `max_tokens: 2000` bounds what one call GENERATES (landed with GAP-009); this adds the input side, which is the larger half of the bill here — every request serialises the period's staff, shifts, leave and availability, and a 250-person org over a fortnight is far more text than the reply. A 2,000-character cap on the manager's prompt, checked before anything else costs anything; a 120,000-character ceiling on the serialised context, **refused rather than trimmed**, because dropping staff to fit answers with a rota that ignores part of the roster and gives no sign of it; the context is now sent compact rather than pretty-printed, which was about a fifth of every request in whitespace nobody reads. A per-ORGANISATION limit of 200/hour joins the per-user 30/hour: the invoice arrives per organisation, and twenty managers inside their own allowances is 600 calls an hour with nobody abusing anything. And OpenRouter's own `usage` figures are written to every audit row — "no spend guard" was two problems, and the second was that nothing knew what had been spent. The model-fallback half is not a cost question and is now CAP-072a / HARDEN-009.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | P1       |
| ~~HARDEN-010~~ | AI function                | 🟢 **Closed 2026-08-31.** The mechanism shipped in #220 and was deliberately left unset, because which model to fall back to has a bill attached. Decided now: **`google/gemini-3.5-flash-lite`**, set and deployed. Three things made it the choice — a **different provider**, since a fallback on the same vendor routes around a model outage and not a provider one; `response_format` support, **checked against OpenRouter's live model list** rather than assumed, because three of the four slugs guessed first did not exist at all and a wrong slug is a fallback that silently never works; and a genuine capability peer, because the cheapest models available are 20× cheaper and much weaker, and a fallback whose suggestions the verify pass then drops spends money to produce a worse answer than a clean 502. It costs roughly 2× the input and 4× the output of the primary, which is the right trade for something that only runs while the primary is down.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| ~~HARDEN-005~~ | Edge functions             | 🟢 **Closed 2026-08-31 (#216), deployed to all seven then live at the time; `calendar-feed` shipped afterwards and makes eight.** 23 `console.error` calls and nothing read them: a React error was triaged in minutes while a Stripe webhook that threw — the one place money and subscription state change — was a line in a log nobody opens. `_shared/sentry.ts` posts to Sentry's envelope endpoint directly rather than importing `@sentry/deno`, which is a large dependency for one POST in a runtime where a bad import is a 500 on a live endpoint. It never throws, never blocks, no-ops without a DSN, and carries no user content. Wired into every top-level catch plus three failures that are invisible AND consequential: a Stripe signature rejection, an invite created but never delivered, and a delivery-log insert that fails after the notification went out. Deliberately not wired into expected refusals — a reporter that fires on those makes the noise that is why nobody reads the log now.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | P2       |
| ~~HARDEN-006~~ | Query shape                | ✅ **Closed 2026-08-30.** `0072` adds 16 composite indexes, each named against the function that issues its predicate; the builder resolves every location's rota in one query and their shifts in one more, so changing the week is 2 round trips rather than 2N. Found and fixed en route: copy-previous-week called `getOrCreateRotaForPeriod` on a **past** week, writing an empty rota into any week a site had not been scheduled for. `src/services/rotaBatchLoading.test.ts`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     | P2       |
| HARDEN-007     | Scale                      | Never tested beyond a single-org tenant                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  | P3       |
| ~~HARDEN-008~~ | Notification authorisation | 🟢 **Closed 2026-08-31 by deleting the path, not by adding a check.** The residual this row named was same-tenant abuse: the browser posted to Inngest with a public, Vite-inlined event key, `supabase/functions/inngest` forwarded `event.data` verbatim, and any member could therefore send their own colleagues a notification with a fabricated type and title, indistinguishable from management. Cross-tenant was already closed by the membership filter; this half was not. `0087` moved every dispatch into the database, so nothing legitimate had used that path since — it was pure attack surface. The `inngest` Edge Function is deleted and undeployed, `VITE_INNGEST_EVENT_KEY` no longer ships (**this sentence was untrue for four days — see HARDEN-010**), `notificationDispatchService` is gone, and `send-notification` now accepts **only** the `vault` secret (the one-release environment-variable fallback is removed, that window having closed). The only caller left is the pg_cron drain, sending rows written by triggers — a payload no client can author.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ~~HARDEN-009~~ | Session control            | 🟢 **Closed 2026-08-31 by `0100` (#238), and the weakness was half-imagined.** This row said there was no server-side registry. There always was: `auth.sessions` is GoTrue’s own table and has carried `created_at`, `refreshed_at`, `user_agent` and `ip` per session since the project existed — nothing surfaced it, which is a much smaller problem than the one written down. `my_sessions()` and `revoke_my_other_sessions()` both filter on `auth.uid()` as their first condition and take no session id, so there is no argument that could widen the set, and `/app/account/sessions` renders them. It matters most for the case this product actually has: a clock-in tablet by a door stays signed in after whoever used it leaves.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | P3       |
| ~~HARDEN-011~~ | Retired-service keys       | 🟢 **Opened and closed 2026-08-31.** `0087` retired Inngest and HARDEN-008 deleted its Edge Function, its hook and its service — and the register, `docs/ARCHITECTURE.md` and this function’s own header all recorded that "the event key no longer ships". It shipped for four more days. Vite emits `import.meta.env` as one object, so every `VITE_*` variable in `.env` is inlined whether any code reads it or not, and `VITE_INNGEST_EVENT_KEY` sat in `dist/assets/index-*.js` on a public site with a real value in it. Removed from `.env` and `.env.example`, rebuilt, and verified absent by grep over the built bundle. **Deleting a code path is not deleting its credential**, and only the artefact can tell you which one you did. Rotating the key at Inngest is the owner’s remaining step, and now the load-bearing one. The deploy on 2026-08-31 removed the key from the origin — verified over SSH that no `inngest` chunk remains, and the live `index-*.js` contains no `INNGEST` — but Cloudflare had already cached the old chunk under `cache-control: public, max-age=31536000, immutable`. It reads `cf-cache-status: HIT` at ~2.7 days old and will keep serving the key from the edge for up to a year unless purged, and no purge token exists on the deploying machine. **Rotating the key makes the cached copy worthless in a way a purge alone would not**: a year-long immutable cache is precisely why shipping a new artefact does not undo an artefact leak. Two residues also outlived the code — `https://inn.gs` in the CSP `connect-src` — the last place the retired service was named anywhere, and found only by reading the live response headers rather than the repository. Removed from the repo `.htaccess` and carried to the origin the same day by a composed `--with-htaccess` deploy, the origin-lock block spliced back on top and thirteen properties asserted on the composed file before anything was uploaded. Verified after: root, a deep route, the bundle and `security.txt` all 200, direct-to-origin 403, `/.well-known/` still exempt so certificate renewal survives, and `connect-src` no longer naming the host. | P2       |
| ~~HARDEN-012~~ | Platform authorisation     | 🟢 **Opened and closed 2026-09-04 by `0116`.** `delete_organisation`, `connect_integration` and `set_org_integration_status` authorised on `is_platform_admin()`, which is true for all four platform roles. Everywhere else the pattern is `has_platform_role(array[…])` with the roles written out. `delete_organisation` is granted to `authenticated` and there are no backups, so the console hiding the button from a finance-role account was the only thing between it and a permanently deleted tenant. Eight pgTAP assertions pin who can and cannot. **CI's `db-tests` job runs them and they pass** (387 assertions across 44 files, up from 379). The first attempt failed on the fixture rather than the guard, and it found something: `0073` set `available = false` on all eight seeded connectors, so `connect_integration` refuses every call on availability grounds before the role check is reached, and `set_org_integration_status` has no row to act on. Those two are latent — wrong, and unreachable until a connector is made available. **`delete_organisation` is not latent**, and it is the one that destroys a tenant. The read half is GAP-053.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | P1       |
| ~~HARDEN-013~~ | Build-time configuration   | 🟢 **Opened and closed 2026-09-04.** `src/lib/env.ts` read `import.meta.env[key]`, a dynamic index Vite cannot statically replace, so it emitted the entire env object and the bundle carried every `VITE_*` in the builder's `.env` — including a live Stripe publishable key that appears in no source file. Keys are now named statically, and `scripts/check-bundle-size.mjs` fails the build on any undeclared `VITE_*` in `dist/`. Closing the class rather than the instance: HARDEN-011 removed one key and left the mechanism that admitted it.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | P1       |

## §9 Not building, and why

| Item                                             | Reason                                                                               |
| ------------------------------------------------ | ------------------------------------------------------------------------------------ |
| SMS                                              | No provider, no delivery record. A toggle that sends nothing is worse than no toggle |
| Per-tenant sending domains with DNS verification | BYO-SMTP already achieves it — see §5                                                |
| Autonomous scheduling                            | Deterministic logic decides; the model only phrases. Do not invert that              |
| Dedicated sending IPs                            | No volume to justify it, and reputation is already per-tenant under BYO-SMTP         |
| Marketplace                                      | Requires an API and integrations that do not exist                                   |
| Internationalisation                             | UK-first until the UK product is excellent                                           |
| Public status page                               | A second surface with its own hosting and its own failure mode                       |

## §10 Roadmap

**P0 — nothing ships to a paying customer until these close.**

Closed 2026-08-29: ~~unsynced writes reported as success~~ (BUG-043, BUG-044, #162) ·
~~`STRIPE_MODE` defaulting live~~ (BUG-052, #163) · ~~preference controls read by nothing~~
(BUG-048, #165) · ~~push with no handler~~ (BUG-050, #165) · ~~the data-residency claim~~
(BUG-056, #161).

Closed since: ~~notification dispatch was fire-and-forget~~ (BUG-047, #166).

Still open: backups and one completed restore (GAP-001) · `event_at` silently rewritten past
72h (BUG-045 — needs a migration, and migrations auto-apply into a database with no backups) ·
one real Stripe charge (CAP-036) · re-run the multi-tenant isolation test (CAP-045) · require
rotate the platform-owner credential onto a
live domain.

**Deploy debt: cleared 2026-08-29.** All eight edge functions are deployed (see §2). Remember
that merging never deploys them; a future edge-function fix needs `supabase functions deploy`
naming it, or the register will claim something production does not have.

**P1 — reliability and communications as a system. All closed as of 2026-08-31,
with one exception.**
~~Server-side notification dispatch~~ (GAP-026, #200) · ~~delivery outcomes persisted~~ ·
~~preferences read at send time~~ · ~~minimum cover enforced in `publish_rota`~~ ·
~~rate limiting and an AI cost cap~~ (GAP-009 #199, HARDEN-004 #204) · ~~ground the AI
announcement output~~ (BUG-058, #202) · ~~amendment diff~~ (GAP-007, #185) · ~~invite emails~~ ·
~~outbox idempotency and a retry path~~. Reason-for-change and acknowledgement follow the diff
and are not yet built. **Real-device UAT remains open and cannot be closed from here** — it needs
a phone, a person and a signal that drops.

The queue itself was the last thing to work: it had delivered nothing since 29 August because a
shared secret had to be set identically in two places that cannot be read back. `0091` generates
it inside Postgres instead (#210).

**P2 — entitlements, truthfulness, coverage.**
Closed 2026-08-31: ~~every entitlement gates something or stops being one~~ (BUG-064, #208) ·
~~URL schemes constrained in the database~~ (HARDEN-003, #213) · ~~notification retention~~
(GAP-027, #213) · ~~scheduled alerts for missed clock-ins and expiring documents~~ (GAP-013,
#214) · ~~an abandoned onboarding wizard resumes~~ (GAP-015, #218) · ~~Edge Function errors
reach Sentry~~ (HARDEN-005, #216) · ~~the pricing page cannot drift from the plans table~~
(BUG-053, #219) · ~~colour contrast at zero in both themes~~ (GAP-030 #206, GAP-032 #207) ·
~~the auth configuration is read and watched~~ (GAP-031, #209) · ~~a test that actually signs in~~
(GAP-010, #222).

Also closed 2026-08-31: ~~ownership transfer and duplicate records~~ (CAP-091, CAP-092, #226) ·
~~a dead-lettered write can be retried~~ (CAP-016, #228) · ~~`shift_templates` removed rather than
built~~ (BUG-051, #229) · ~~the first component tests~~ (CAP-100, #230) · ~~a transient first load
no longer greets a new customer with an error card~~ (#227, found by the E2E test added in #222).

Still open: optimistic concurrency on approvals · the builder N+1 ·
CAPTCHA (GAP-033) and leaked-password
protection (GAP-034), both put to the owner on 2026-08-31 and deliberately deferred · a fallback
model chosen for the AI (HARDEN-010 — the mechanism ships, unset) · the DPA once counsel drafts
it · label the audit actions.

**P3 — after product-market fit.** Everything in §7 marked P3, in the order the maturity ladder
implies.

## Drift Audit Log

Written by `.github/workflows/plan-drift-audit.yml` (weekly). Each entry starts
`**YYYY-MM-DD**:` on its own line — required for the workflow's own staleness check to parse the
log.

**2026-08-13**: Seeded at creation. No automated audit has run yet — the workflow needs an
`OPENROUTER_API_KEY` repository secret before its first scheduled or manually-dispatched run can
execute. (This originally read `ANTHROPIC_API_KEY`; the audit was moved to OpenRouter on
2026-08-20 so that this project depends on a single AI provider — the same one the app's own
`ai-rota-assistant` Edge Function uses.)

**2026-09-05 (second entry, reliability repairs)**: An external delivery audit of the working
copy raised fourteen findings. RF-01 was already fixed by #292-#294 and needed nothing. Eleven of
the remaining thirteen are closed here as BUG-071 to BUG-081, each with a regression test that
fails without its fix; three are recorded as GAP-071 to GAP-073 because they are blocked on a
credential, a browser harness or a dataset rather than on work, and GAP-074 records a deployment
ordering constraint the repairs created.

Four things are worth carrying forward from doing it.

**The migration replay caught a bug three parse checks did not.** `0123`'s backfill read
`public.audit_events`. There is no such table — the audit log is `audit_logs`, and has been since
`0016`. `libpg-query` parses `audit_events` perfectly happily, because it is a syntactically valid
identifier. Only `supabase db reset` against a real Postgres said otherwise. Docker was available
this session, which is not usually true here, and that is the whole reason the error was found
before merge rather than in production, where migrations apply automatically and there are no
backups (GAP-001).

**One finding was worse than reported, in the opposite direction.** The audit said a night shift
contributed 0 minutes to the period it ended in. True, and by itself a fifty-fifty question of
allocation policy. The half it did not reach is that the period the shift STARTED in received the
`in` without its `out`, so `pairClockEvents` closed the segment against `now` — a report run three
days later paid three days, and set no review flag, because as far as the function could tell the
person was still clocked in. A missing number is visible. An invented one is not.

**A test that addresses fixtures positionally can assert against the wrong row.** The first draft
of `swap_decision_atomicity.test.sql` selected swaps with `order by created_at desc limit 1`.
Every row in a pgTAP file is inserted inside one transaction, so `created_at` is identical on all
of them and that ordering is arbitrary. Three assertions failed and read exactly like a failure of
the migration. Fixtures are addressed by `note` now, and the reason is written at the top of the
file.

**The gate that was supposed to cover migrations was passing vacuously.** `check:migrations`
diffed `origin/main...HEAD` with the base hard-coded, so run on `main` it printed "No new
migrations in this branch" — indistinguishable from a real pass. It now names its base, says when
the comparison was empty by definition, fails closed on an unresolvable ref, and refuses modified
or deleted migrations, which it never looked at before. That last one matters: editing an applied
migration changes what a rebuild becomes while leaving production alone, which is how `0113` came
to be needed.

**2026-08-20**: The audit revealed drift in the product plan document, specifically regarding the
status of the 'Run an RLS/destructive-action review on deployed Supabase' task, which is marked
as 'Not done' in the document but has evidence of being completed in the recent git history.
Additionally, the task 'Test the critical loop on real mobile devices' is also marked as 'Not
done' but lacks evidence of completion. The legal page for AI transparency is missing, which was
noted as a gap in the document.

**2026-08-24**: The audit revealed drift in the product plan document, specifically regarding the
status of the 'Run an RLS/destructive-action review on deployed Supabase' task, which is marked
as 'Not done' in the document but has evidence of being completed in the recent git history.
Additionally, the task 'Test the critical loop on real mobile devices' is also marked as 'Not
done' but lacks evidence of completion. The legal page for AI transparency is missing, which was
noted as a gap in the document.

**2026-08-29**: This file replaced the previous transformation-plan v2 as the audited
document. Five planning documents were merged in and deleted after nine read-only audits of the
repository. Sixty-plus contradictions were found between the documentation and the code,
including eight places where two documents contradicted each other and one — the data-residency
statement in `docs/DATA_LIFECYCLE.md` — that was feeding a customer-facing Privacy Notice.
`scripts/plan-drift-audit.mjs`'s system prompt was rewritten at the same time: it named V2's
section headings literally, so pointing it at this file without that change would have returned
`drift_found: false` against a register it never read.

**2026-08-31 (second pass, by hand rather than by the weekly job)**: A consistency sweep across
every reference document after fifty-five pull requests had landed since the register was last
recounted. It found the register's own §0 rule broken in both directions and one live page telling
a customer something untrue.

- **A capability shipped and its gap stayed open.** CSV import landed as CAP-084 in #242 and
  GAP-022 still read "No CSV import". The same pull request should have struck it; that is the
  whole rule.
- **Two closed items were still listed as work.** GAP-012's SLA half closed with `0110` and
  HARDEN-009 closed with `0100` — `auth.sessions` had carried a per-device registry the entire
  time and nothing had surfaced it, so the weakness as written was half-imagined.
- **An evidence path that did not resolve.** CAP-023a cited the push service worker with the
  two halves of its filename transposed — sw-push rather than `public/push-sw.js`. The weekly
  audit resolves these paths and would have caught it; nothing had run since it was written.
  (Written out rather than quoted as a path, so this entry does not itself become a MISSING
  verdict every week.)
- **`/legal/trust` named a sub-processor that receives nothing** (BUG-067), citing a service file
  deleted with `0087`'s dispatch path. Now checked on every push rather than weekly: `npm run
check:docs` resolves the repository paths in `src/lib/subprocessors.ts` and fails on one that
  does not exist.
- **A retired credential kept shipping** (HARDEN-011, renumbered 2026-09-02 — it was written as a second HARDEN-010). `VITE_INNGEST_EVENT_KEY` sat in the public
  bundle for four days after every reader of it was deleted, because Vite inlines the whole of
  `import.meta.env`. Three documents and one function header stated it did not.
- **A comparison against a value the column cannot hold** (BUG-066): `/app/help` tested
  `author_side === 'agent'` where `0024` allows only `'customer'` and `'platform'`, so every
  support reply was labelled with the agent's own name.

Two figures in §2 were stale in the flattering direction — "twenty-eight pull requests (#178-#210)"
and a hardcoded row count of 111 against a table summing 113. The count sentence was deleted rather
than corrected: `npm run check:docs` prints it and fails when it disagrees, so writing it here again
would recreate exactly the number nothing checks.

The pattern across all seven: **every one was a claim nothing executed.** The counts that are
checked by a script were all correct.

**2026-08-31 (third pass — the gap register, decided rather than listed)**: §7 held four rows
that had sat open for weeks with nobody working on them and nothing recording why. Two are now
closed and two are decisions.

- **GAP-029 closed by the option it named itself.** `npm run check:docs` reads
  `security.txt`'s `Expires`, warns at 90 days and fails at 30. RFC 9116 §2.5.5 says a consumer
  MUST ignore the file past that date, so expiry is not staleness — it is having no published
  security contact, silently, until a researcher has nowhere to send a report. Verified green at
  363 days and red when the date is moved inside 30.
- **GAP-024 deferred on evidence.** The work is small — the probe writes real samples, the
  column exists, it needs an `anon` policy and a route. The reason to wait is that production has
  no customers, and a status page reporting "operational" from a probe against an unused platform
  is an untested green light that will be believed the first time it is wrong. Reopens at the
  first paying customer. Shipping it means granting `anon` its first read since `0075`; that
  grant is the decision, not the page.
- **GAP-025 handed back.** Every other row here can be settled by reading the code. Whether to
  build a vertical configuration surface cannot be, and leaving it in a list implied engineering
  was working through it. Reopens when a named customer is lost for a specifically vertical
  reason.

**The remaining open rows are not tasks anybody here can do.** GAP-001 needs a billing decision,
GAP-033 and GAP-034 were put to the owner and declined, GAP-035 needs an inbound mail route, and
every ❓ needs a live environment: a restore, a card, a phone. That is a more useful thing for
this section to say than a longer list.

**2026-08-31 (fourth pass — what is not needed, checked rather than assumed)**: The earlier
passes deleted the obvious things: a folder of unreferenced screenshots, and local scratch. A
proper sweep found a second kind, and it was larger.

**Twelve modules in `src/`, 1,364 lines, that nothing imports.** Verified by resolving every
non-entrypoint module against every import in `src/` and `e2e/`, including string and dynamic
references, not by reading. The largest was `ShiftInspectorPanel.tsx` at 534 lines; the rest
were a marketing grid, an industry strip, a coverage ring, a table paginator, a popover, two
staff cards, a timesheets quick-actions card, a rota action rail, an availability matrix and a
hook.

**Two of them were edited on 2026-08-30 by the dark-mode contrast sweep** (#207) — a11y work
carefully applied to components nobody renders. That is the cost of dead code stated precisely:
not bundle size, which tree-shaking already handled, but that it is indistinguishable from live
code to every person and every tool that touches the repository afterwards.

`useOptimizedImage` is the one worth naming separately, because it was **documented as an
approved hook contract in `docs/HOOKS.md`** while no component called it. A contract nobody
consumes reads as a rule about how images must be loaded; it was a 15-line memo around
`buildImageKitUrl`, which every real caller uses directly. `src/lib/imagekit.ts` is unchanged.

The decision is `0096`'s, applied one layer up: **remove rather than keep speculative
structure.** Building readers for things nothing reads is how `shift_templates` sat inert for
two months. Everything deleted here is in git history if a use ever appears.

Nothing else is unreferenced. Every file in `docs/design/` is cited by a document, every file
in `public/` is referenced by the app or the build, and the module scan now returns empty.

**2026-08-31 (fifth pass — the two checks nobody had looked at)**: The previous passes
reconciled this file against the repository, the database and the served site. The one place
left was **GitHub Actions**, and it held the worst finding of the day.

`backup.yml` and `auth-config.yml` are the two gates that read live state no pull request can
change. Both have now run — each on its first schedule — and **both failed.**

```
backup:      Backups are not configured. Missing secret(s): SUPABASE_DB_URL BACKUP_PASSPHRASE.
             Until these exist there is no backup of production at all.
auth config: SUPABASE_ACCESS_TOKEN is not set, so the auth configuration was NOT checked.
```

`gh secret list` returns exactly one secret for this repository: `OPENROUTER_API_KEY`.

Three things follow, and each was written down here as true:

- **GAP-001 led with 🟡 "Mitigated"** and put "it is inert until two secrets exist" sixty words
  later. A reader scanning status glyphs concludes the largest risk on the list is handled. Zero
  backups of production have ever been taken, and #259 rehearsed restoring a file that is never
  produced. The row is 🔴.
- **GAP-031 said the auth rate limits are "read and asserted by `scripts/check-auth-config.mjs`".**
  The script is real; the workflow running it has never succeeded. The values in that row were
  read by hand. Nothing is watching them.
- **`CLAUDE.md` predicted this exactly** — "a check that passes when it cannot see anything is
  worse than none, because it is believed" — and both checks behaved correctly, failing loudly.
  The design was right and it did not help, because **a scheduled workflow fails where nobody is
  standing**: it blocks no merge, marks no pull request red, and appears on no screen anyone
  opens. A merge gate is seen whether or not you look for it; a cron gate is not.

Tracked as GAP-036, P0, and it cannot be closed from here — all three secrets are credentials
only the owner can mint. What this pass could do is stop the register saying otherwise.

That is the whole session in one entry: **nine times out of nine, the defect was a claim nobody
had executed.** The register now says what was measured, and names who has to act where the
answer is not code.

**2026-08-31 (sixth pass — the live catalogue, and the Supabase advisors)**: With the Actions
tab checked, the last unread source was the project's own security advisors. Zero ERROR-level
findings. The WARNs resolved into three things, and only one was a defect.

- **`rls_enabled_no_policy` × 2** — `platform_health_probes` and `rate_limit_events`, documented
  as deliberate in `docs/SCHEMA.md` §4.8 an hour earlier. The advisor agreeing with a decision
  already written down is the outcome to want from a lint.
- **`auth_leaked_password_protection`** — GAP-034, put to the owner and declined. Unchanged.
- **`anon_security_definer_function_executable` × 15** — the real one, and the advisor
  overstates it: 23 of the 24 anon-executable functions return `trigger` and cannot be invoked
  over PostgREST at all. Excluding those leaves three, and **two should not be there**
  (HARDEN-002, `0112`).

The root cause is worth more than the instance. `0075` swept every function and then wrote
`alter default privileges ... revoke all on functions from anon`, which **cannot do anything**:
the built-in default grants EXECUTE to `PUBLIC`, and `anon` inherits through it, so revoking a
default "from anon" removes an entry that was never there. Twelve migrations later two had
slipped, neither exposing data — which is why nothing noticed, and why the fix is a default
and a pgTAP invariant rather than two revokes.

**Also verified live in this pass, and healthy:** four `pg_cron` jobs, not the three
`CLAUDE.md` listed — `rotaflow-scheduled-alerts` (`0093`) was missing from that list. Failure
counts from `cron.job_run_details`: outbox 0/2590, health probe 0/351, alerts 0/65, retention
14/25 — and all fourteen are the pre-`0057` "column reference data_type is ambiguous" error,
last seen 2026-08-20, with every run since succeeding. Recorded because **an `active` job and a
working job are different claims**, and this file has now been caught making the first while
meaning the second twice in one day.

**2026-08-31 (seventh pass — the dependency nothing imports)**: `framer-motion`, 5.6 MB, in
`dependencies` since the project began and **imported by nothing**. Three places carried it
forward as if it were in use:

- `README.md`'s tech-stack table sold it to a reader as "Motion — micro-interactions and page
  transitions". The product has neither, from that library.
- `vite.config.ts` reserved it a `manualChunks` entry, `motion: ['framer-motion']` — a chunk
  that could never be emitted, because nothing imports the thing it names.
- `docs/DESIGN.md` offered CSS as "the fallback when Framer Motion isn't warranted", which
  inverts what is actually true: CSS is the only animation mechanism here.

The only accurate mention was a comment in `HomePage.tsx` explaining why a JS animation library
is **not** used — an entrance animation that starts at `opacity: 0` leaves the most important
copy on the site invisible to a slow phone, blocked JS, or a crawler, which is a bug this
project has already had once and caught by screenshotting rather than by testing.

Removed, along with all three claims. Verified with `rm -rf node_modules && npm ci` rather than
`npm install`, because the two are not the same check — an install rewrites the lockfile and
would hide exactly the breakage this could cause in CI.

**This is the last of the "what is not needed" sweep**, and worth stating as a finding rather
than a chore: a dependency is the one kind of dead weight that is invisible to every check this
repository runs. It typechecks, it lints, it tree-shakes out of the bundle so no budget notices,
and it costs 5.6 MB on every `npm ci` in every CI job. The only thing that finds it is asking
which declared dependencies nothing imports.

**2026-08-31 (eighth pass — the two Dependabot pull requests nobody could merge)**: #155 and #157
had been open since 24 August, both failing, and **both were unmergeable by construction**: each
bumped one half of a lockstep pair. #157 raised `@eslint/js` to 10 while the project pins
`eslint@^9`; #155 raised `vitest` to 4 while leaving `@vitest/coverage-v8` at 3. Neither could
ever go green, and neither said so — they simply sat red for a week.

Redone as pairs from current `main`: **vitest and `@vitest/coverage-v8` both to 4.1.11**, plus
every in-range update `npm update` would take (`@sentry/react` 10.73.0, `@supabase/supabase-js`
2.112.4, `lucide-react` 1.38.0, `react-router-dom` 7.18.3, `typescript-eslint` 8.69.0,
`@types/node` 26.4.0). Verified with `rm -rf node_modules && npm ci`, not `npm install` — the
distinction that makes a lockfile change real. 808 tests pass under vitest 4, and the entry
chunk fell from 151 KiB to 132 KiB gzipped as a side effect.

**eslint 10 was not taken, and the reason is outside this repository.**
`eslint-plugin-jsx-a11y`'s latest release peers only to eslint 9. Taking eslint 10 means
`--legacy-peer-deps` and an accessibility ruleset running unsupported, in a project that spent
two days getting contrast to zero. That is GAP-037, with a per-package reopening condition
rather than a silent skip.

**2026-08-31 (ninth pass — the two files nobody opens)**: `docs/ORGANISATION_WORKSPACE.html` and
`docs/PLATFORM_CONSOLE.html` are 276 KB between them and are named as the sole reference for the
Organisation section and the console. Neither had been read in this reconciliation, because they
are mockups rather than prose — ~170 characters of text each, the rest script and style.

One of them was wrong in a way that matters. **The console header read `eu-west-2`.** The
project runs in `eu-west-1`, read from the Supabase Management API rather than from a document,
and `/legal/trust` publishes "eu-west-1 (Ireland)" to customers as a **data-residency
statement** — with `src/lib/subprocessors.ts` and `src/lib/legalFacts.ts` saying the same.

A wrong region in a mockup is not a typo. It is the design reference somebody builds the console
header from, and building it faithfully would have put a region on screen that contradicts the
product's own privacy notice. Corrected, with the reason left in the file as an HTML comment so
the next person does not "fix" it back.

Checked at the same time and clean: every one of the 60 routes `docs/SCREENS.md` cites resolves
in `App.tsx`, and every component it names exists — including `AuthCallbackPage`, which lives in
`RouteAliases.tsx` rather than a file of its own, exactly as that document says.

**2026-08-31 (tenth pass — the remaining documents, each checked against something)**: With the
mockups read, the files that had only been skimmed were audited against a source rather than
against themselves. All four hold up, and recording that is the point: a reconciliation that only
reports defects gives no way to tell "checked and correct" from "not checked".

- **`docs/SCREENS.md`** — all **60** routes it cites resolve in `App.tsx`, and every component it
  names exists, including `AuthCallbackPage`, which lives in `RouteAliases.tsx` rather than a file
  of its own, exactly as the document says.
- **`docs/OBSERVABILITY.md`** — all **15** columns it names as computing the "Computable now"
  metrics exist in the live database. Its four "needs new capture" rows are still true; nothing
  records a schedule view, and no Web Vitals collection exists.
- **`docs/BRAND.md`** — the approved tagline matches `src/lib/brand.ts` byte for byte,
  `BrandMark` is real and used in 12 files, and the evidence boundary holds: `TRACTION` and
  `TESTIMONIALS` are still empty arrays.
- **`docs/HOOKS.md`** — every file in `src/hooks` has a section. Two sections describe hooks that
  no longer exist, and a note now says so at the top, because this pass flagged them as stale
  before recognising them as deliberate tombstones. A document whose own convention trips its own
  audit is one edit away from somebody deleting the explanation to make a listing line up.

That is every file in `docs/` checked against the repository, the live database, the served site,
the Actions tab or the Supabase catalogue — whichever could actually falsify it.
